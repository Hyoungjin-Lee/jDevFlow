# jDevFlow v0.3 Kickoff — Pre-Session State Snapshot

Date: 2026-04-21
Owner: Jonelab_Platform / jDevFlow
Purpose: record everything that is **already done** going into the first v0.3 development session, so that session does not redo Stage 0 work by accident.
Entry prompt: `prompts/claude/v03_kickoff.md` (Korean primary, English alternative).

---

## 1. What v0.2 shipped (before today)

- Tiered workflow model **Lite / Standard / Strict** introduced in `WORKFLOW.md` v2.0
- Stage types, execution conditions, completion criteria, re-entry rules documented
- `prompts/claude/*.md` upgraded to mode-aware v0.2 across brainstorm, planning (draft/review/final), technical design, code review, final review, QA
- `.skills/` behavior-contract library created (`README.md`, `_templates/behavioral-skill-template/`, `examples/safe-script-run/`)
- `docs/notes/session_bootstrap.md` and `workflow_eval_plan.md` drafted
- `ATTRIBUTION.md` — credit to `obra/superpowers`
- `HANDOFF.md` template extended with `mode` / `has_ui` / `risk_level` fields

---

## 2. What today's Stage 0 session locked down (v0.3 engine pre-work)

### 2.1 Resolved decisions — D-A ~ D-D

Confirmed with the user (see `prompts/claude/v03_kickoff.md` → "확정 결정 / Resolved decisions"). These enter v0.3 Stage 1 Brainstorm as **premises**, not open questions.

- **D-A.** UI-stage role: **Model A** — Claude writes `ui_*.md` spec → user/tool builds UI → Claude reviews it as the Stage 9 UI counterpart. MCP-direct driving reserved for `ui_auto` flag in v1.0.
- **D-B.** Recommendation logic lives in **`.skills/tool-picker/SKILL.md`**. No hard-coded if-then inside prompts.
- **D-C.** Custom entry storage is **B + C hybrid**: `.claude/tools.json` (project-scope catalog, persistent) + `HANDOFF.md` "current tools" field (session state, current selection). Global user profile `~/.claude/tools.json` is v1.0 scope.
- **D-D.** Stage 10 re-ask is **conditional**: re-run tool-picker only when Stage 9 verdict is design-level (architecture / schema / UI-flow rework). Minor / bug-fix verdicts keep the Stage 8 executor.

### 2.2 Files created or updated in Stage 0

- `.skills/tool-picker/SKILL.md` — full 8-section behavior contract + Recommendation Matrix (executor axis, UI-tool axis, Stage 10 re-eval table). `change_size` uses `small | medium | large` enum with a reference line-count mapping.
- `.claude/tools.json` — scaffold (empty catalog) with a schema note. Present so tool-picker first-run doesn't hit a missing file.
- `prompts/claude/brainstorm.md` — added a forward-reference blockquote under "After writing this document" pointing to the Stage 5 tool-picker consultation (Lite mode: consultation happens at Stage 8 entry instead). Explicitly says **do NOT call tool-picker at Stage 1**.
- `prompts/claude/v03_kickoff.md` — Open Questions block replaced with Resolved Decisions D-A ~ D-D. Goal list extended to 12 items. Opening questions updated.
- `README.md` and `README.ko.md` — v0.3 roadmap row contains all 12 items.

### 2.3 What Stage 0 is NOT

This Stage 0 work was done **outside** the canonical Strict Flow (Stages 1–13) because it was an engine-decision pre-requisite for v0.3 itself — the template applying its own governance to its own upgrade. Stage 0 has no counterpart in `WORKFLOW.md §6` and should not be confused with brainstorming or planning.

---

## 3. What v0.3 still has to do (session-1 scope candidates)

Twelve goals, not to be attempted all at once. See `prompts/claude/v03_kickoff.md` → "v0.3 goals" for details.

1. First-run language-selection wizard (`scripts/init_project.sh`, `.claude/language.json`)
2. Automated workflow-eval runner (`tests/evals/*.yaml`, `scripts/run_evals.sh`)
3. Session-bootstrap hook reference implementation (Claude Code / Cowork)
4. `.skills/examples/` expansion (api-client, report-generator, safe-deploy)
5. `HANDOFF.md` auto-writer script (`scripts/update_handoff.sh`)
6. Mode-selection decision-tree skill (`.skills/mode-picker/SKILL.md`)
7. Stage 6 / 7 UI prompts upgraded to v0.2 parity (Runs-in / completion / re-entry)
8. `ai_step.sh` + `zsh_aliases.sh` mode-aware CLI (`--lite` / `--standard` / `--strict`)
9. `CHANGELOG.md` (Keep a Changelog format) + discipline rule
10. `CONTRIBUTING.md` split + Contributor Covenant `CODE_OF_CONDUCT.md`
11. Stage 8 implementer picker (wired through tool-picker skill)
12. Stage 6 / 7 UI-tool picker (Antigravity / Claude Design / Figma / Custom — wired through tool-picker)

Goals 11 and 12 are **partially pre-wired** by the tool-picker skill and D-A ~ D-D decisions. Their Stage 5 Technical Design is already partly done (the Recommendation Matrix). What remains: connect the matrix to `prompts/claude/technical_design.md`, `ui_requirements.md`, `ui_flow.md`, and the Stage 8 entry prompt.

Goals 2, 5, 8 contain real Python/Shell code — Stage 8 must go through Codex (per WORKFLOW.md §6).

---

## 4. Entry sequence for the new session

1. Open a fresh Claude session.
2. Paste the copy-paste block from `prompts/claude/v03_kickoff.md` (Korean primary or English alternative).
3. Claude reads: `CLAUDE.md` → `HANDOFF.md` → `WORKFLOW.md` → `README.md` Roadmap section.
4. Claude asks the 4 opening questions (see v03_kickoff.md bottom).
5. Session proceeds to Stage 1 Brainstorm of v0.3 itself, treating v0.3 as a **Strict-mode meta-project** (the template applied to itself).

If Claude tries to re-open D-A ~ D-D as questions, stop and point it to this file + v03_kickoff.md → Resolved decisions.

---

## 5. Known gaps / deferred to v1.0

- Global user profile `~/.claude/tools.json` for cross-project preferences — out of scope for v0.3
- MCP-direct UI driving (Model B) — gated behind `ui_auto` flag, not implemented in v0.3
- Automated threshold tuning of the `change_size` enum — first 10 real runs will provide calibration data via `docs/notes/workflow_eval_plan.md`

---

## 6. References

- `prompts/claude/v03_kickoff.md` — the actual session entry prompt
- `.skills/tool-picker/SKILL.md` — the tool-picker engine (Stage 5/6/8 input)
- `prompts/claude/brainstorm.md` — Stage 1 prompt (contains Stage 5 forward-reference)
- `README.md` / `README.ko.md` — Roadmap section with all 12 v0.3 items
- `WORKFLOW.md` — tiered model + 13-stage canonical flow (unchanged from v0.2)
- `docs/notes/2026-04-21-workflow-architecture-handoff.md` — the v0.2 architecture handoff that preceded this
