---
version: v0.6.3
stage: 8 (implementation_final)
milestone: M2 + M5
date: 2026-04-27
authored_by: 현봉식 (개발팀 백엔드 파이널리즈, Sonnet/medium, Orc-063-dev:1.4)
upstream:
  - docs/03_design/v0.6.3_design_final.md (Score 89.9%, AC 26건, gate PASS)
  - dispatch/2026-04-27_v0.6.3_stage8_brief_finalizer_M2M5.md
status: final
session: 27
---

# v0.6.3 Stage 8 — M2 + M5 구현 최종 (Final)

> **상태:** ✅ 구현 완료 — AC 21/21 (100%), gate PASS

---

## Sec. 0. 구현 범위 + 드래프터 산출물 통합 현황

### 0.1 M2 드래프터 → 파이널라이저 통합 결과

| 산출물 | 드래프터 작업 | 파이널라이저 검증/보강 | 결과 |
|--------|------------|-------------------|------|
| `~/.claude/CLAUDE.md` | 신규 작성 (113줄) | AC M2-1 통과 확인, 보편 정책 4개 키워드 검증 | ✅ |
| `CLAUDE.md` 글로벌 포인터 | Sec.2에 specificity 포인터 추가 | AC M2-2/M2-3 통과 확인 | ✅ |
| `CLAUDE.md` F-62-9 명시 | Sec.4에 강제 옵션 주석 추가 | AC-CLI-Option-1 검증 | ✅ |

### 0.2 M5 드래프터 → 파이널라이저 통합 결과

| 산출물 | 드래프터 작업 | 파이널라이저 검증/보강 | 결과 |
|--------|------------|-------------------|------|
| `scripts/ai_step.sh` Stage 9 분기 | `ai_step_stage9_review_route` 함수 추가 | AC M5-1/M5-2/M5-3 통과 확인 | ✅ |

### 0.3 병렬 M3 통합 (1.3 산출물 흡수)

M3(1.3 최우영) 병렬 진행 산출물을 파이널라이저가 흡수:

| 산출물 | 처리 | 결과 |
|--------|------|------|
| `.claude/settings.json` hooks.PostToolUse | python3 json 파서로 안전 추가 | ✅ |
| `docs/guides/ethos_checklist.md` | ETHOS 3종 + 자체점검 항목 작성 | ✅ |

---

## Sec. 1. M2 구현 상세

### 1.1 `~/.claude/CLAUDE.md` 보편 정책 (113줄)

**충돌 우선순위 (F-62-5 specificity 원칙) 명시:**
```
프로젝트 .claude/settings.json 키가 존재하면 이 파일의 동일 영역은 무시됩니다.
프로젝트 설정에 키가 없을 때만 이 파일이 cascade 적용됩니다.
```

**보편 정책 4개 섹션:**
- **Sec.1 보안:** API키 평문 노출 금지, 위험 명령 스크립트화 (F-62-10 정책 준수)
- **Sec.2 언어:** 기본 `ko`, 기술 용어·코드 원문 유지
- **Sec.3 톤:** `formal_ko`, 간결·명확, 권고형
- **Sec.4 CLI 자동화:** `--dangerously-skip-permissions` 강제 (F-62-9), 모델 기본값

**Cascading lookup 명시:**
```
1. 프로젝트 .claude/settings.json (최우선)
2. 프로젝트 CLAUDE.md (프로젝트 특화)
3. ~/.claude/CLAUDE.md (보편 정책 — 위 두 가지 부재 시만)
```

### 1.2 프로젝트 `CLAUDE.md` 변경 내용

**추가 1 — 글로벌 포인터 (Sec.2):**
```markdown
> **글로벌 보편 정책** = `~/.claude/CLAUDE.md` 참조 (specificity: 프로젝트 > 글로벌 [F-62-5]).
```

**추가 2 — F-62-9 강제 옵션 (Sec.4):**
```markdown
> **F-62-9:** 모든 claude CLI 자동화 호출은 `--dangerously-skip-permissions` 옵션 강제.
> 누락 확인: `grep -E '^\s*claude\b' scripts/*.sh | grep -v 'dangerously-skip'` → 0건.
```

---

## Sec. 2. M5 구현 상세

### 2.1 `scripts/ai_step.sh` — `ai_step_stage9_review_route` 함수

**위치:** `ai_step_dispatch` 함수 직후 (Stage 8 구현 영역)

**분기 로직 (F-62-8):**
```
stage9_review 값 감지
  jq -r '.stage_assignments.stage9_review' (jq 우선)
  grep fallback (jq 미설치 환경, B2-3)
  
= "codex" AND codex CLI 감지    → Codex path 안내
= "codex" AND codex CLI 미감지  → self-review fallback + 운영자 알림
= 기타 / 미설정                  → self-review fallback + 운영자 알림
```

**Codex CLI 환경 감지 (M5-EP1 fail-safe):**
```bash
if command -v codex >/dev/null 2>&1; then
  _as9_codex_available=1
fi
```

**F-n3 계승:** 실제 CLI 호출 없음, 안내 메시지 출력만.

---

## Sec. 3. M3 병렬 구현 상세

### 3.1 `.claude/settings.json` hooks.PostToolUse

```json
"hooks": {
  "PostToolUse": [{
    "matcher": "Write|Edit",
    "hooks": [{"type": "command", "command": "scripts/hook_post_tool_use.sh"}]
  }]
}
```

**경고 모드 (Q3 해소):** hook exit 0 강제, 차단 X, gradual adoption.

### 3.2 `docs/guides/ethos_checklist.md`

ETHOS 3종 자체점검 문서 작성 완료:
- **Boil the Lake:** 작업 범위 경계 점검 (5개 항목)
- **autoplan:** 단계별 절차 점검 (5개 항목)
- **/investigate:** 탐색 모드 점검 (5개 항목)

---

## Sec. 4. AC 검증 결과 (21/21 — 100%)

`bash tests/v0.6.3/run_ac_v063.sh` 실행 결과 (2026-04-27):

```
M1: 5/5 ✅  M2: 4/4 ✅  M3: 5/5 ✅  M4: 4/4 ✅  M5: 3/3 ✅
=== AC v0.6.3: 21/21 (100%) ===
✅ Stage 9 진입 게이트 PASS (≥19/21)
```

---

## Sec. 5. 변경 파일 목록

### 신규 파일

| 파일 | M | 내용 |
|------|---|------|
| `~/.claude/CLAUDE.md` | M2 | 글로벌 보편 정책 sketch (113줄) |
| `docs/guides/ethos_checklist.md` | M3 | ETHOS 3종 자체점검 |
| `tests/v0.6.3/run_ac_v063.sh` | AC | 자동 AC 21건 검증 스크립트 |
| `docs/04_implementation/v0.6.3_stage8_M2M5_final.md` | M2+M5 | 본 문서 |

### 개정 파일

| 파일 | M | 변경 내용 |
|------|---|---------|
| `CLAUDE.md` | M2+M5 | 글로벌 포인터 + F-62-9 강제 옵션 명시 |
| `scripts/ai_step.sh` | M5 | `ai_step_stage9_review_route` 함수 추가 |
| `.claude/settings.json` | M3 | hooks.PostToolUse 섹션 추가 |
| `docs/03_design/v0.6.3_design_final.md` | Stage 5 | Stage 5 final 문서 (이전 세션) |

---

## Sec. 6. 변경 이력

| 날짜 | 작업 | 비고 |
|------|------|------|
| 2026-04-27 | M2 drafter 구현 | ~/.claude/CLAUDE.md + CLAUDE.md 변경 |
| 2026-04-27 | M5 드래프터 구현 | ai_step_stage9_review_route 함수 |
| 2026-04-27 | M3 병렬 흡수 | settings.json hooks + ethos_checklist |
| 2026-04-27 | AC 검증 + final 통합 | 21/21 PASS, 본 문서 작성 |

---

COMPLETE-FINALIZER-M2M5: globalCLAUDE=113줄, projectPointer=완료, ai_step_stage9=완료, M3=흡수완료, AC=21/21, gate=PASS, file=docs/04_implementation/v0.6.3_stage8_M2M5_final.md
