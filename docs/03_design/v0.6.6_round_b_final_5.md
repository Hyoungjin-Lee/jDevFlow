---
version: v0.6.6
round: B
agenda: "#5"
topic: spawn_team.sh split panes 보강 + 모델/effort 자동 주입 + @persona 헌법 정합
date: 2026-04-27
mode: Standard
status: final
authored_by: 현봉식 (개발팀 백엔드 파이널리즈, Sonnet/medium, Orc-066-dev:1.4)
upstream:
  - dispatch/2026-04-27_v0.6.6_infra_standard.md (의제 #5)
  - 2e1fd2b drafter(v0.6.6 #5) — spawn_team rewrite + start_claude_team 모델 주입 + personas.sh 신규
  - 77ae46d reviewer(v0.6.6 #5) — R-1~R-3 직접 정정 + R-4 17 테스트 영구화 + R-5/R-6 권고
  - docs/bridge_protocol.md Sec.4 (pane 식별 헌법 표)
session: v0.6.6 Round B
---

# v0.6.6 Round B — 의제 #5 마감 (spawn_team 4-pane + 모델 자동 주입 + @persona)

> **상위:** drafter `2e1fd2b` → reviewer `77ae46d` (R-1~R-6)
> **본 문서:** Round B finalizer reference doc (verbatim 흡수 X — SHA chain만 인용, 헌법 §2.4 정합).
> **상태:** ✅ **PASS — Score 90% ≥ 80% → 의제 #5 완료. Round B 전체 종결 / v0.6.6 릴리스 게이트 OPEN (CHANGELOG + bridge 보고 단계).**

---

## Sec.0. Verdict + Halt 조건

| 항목 | 값 |
|------|------|
| **Verdict** | ✅ PASS |
| **Score** | 90% (≥ 80% 임계 충족) |
| **dispatch halt 조건 (#4 ai_step.sh 호환 FAIL)** | ❌ NOT TRIGGERED — spawn_team이 `settings_require_v04` grace period 사용 (#4 reviewer 인계). |
| **운영자 명시 (HANDOFF Round B 대기 §5 ⚠️)** | ✅ RESOLVED — `start_claude_team.sh` + `spawn_team.sh` 모델/effort 자동 주입 정착. |
| **분량 임계** | drafter 3 file/444 ins-82 del (≤ 800줄) + reviewer 3 file/155 line (≤ 600줄) ✅ |
| **A 패턴 정합** | drafter / reviewer 분리 commit 2건 + R-N 마커 1~6 trail ✅ |

---

## Sec.1. Score 80% 임계 채점

| 가중치 | 항목 | 점수 | 근거 |
|---:|------|---:|------|
| 30 | bridge_protocol Sec.4 헌법 정합 (4-pane + @persona + pane-border) | 29 | reviewer R-1 (`set-option -p @persona`) + R-2 (`pane-border-format` 조건부) + R-3 (`_resolve_persona` show-option 우선) — 헌법 표 line 92 정합. claude CLI auto-rename 면역 확보. |
| 25 | 실측 검증 trail | 25 | reviewer test_personas.sh 17 PASS + spawn_team --dry-run으로 @persona/border/layout/4-pane 전수 확인. finalizer 재실행 시점 17/17 + dry-run 시퀀스 PASS 재확인. |
| 20 | 운영자 명시 사항 해소 (모델/effort 자동 주입) | 19 | drafter `start_claude_team.sh` 페르소나 → 모델 lookup + `--model` 플래그 + 환경변수(`CLAUDE_PERSONA_*`) 자동 주입. T1 18명 누락 0 + T5 이형진 CEO 특수 처리 PASS. |
| 15 | 모듈 분리 (lib/personas.sh 신규) | 14 | persona_model/effort/role/team 4 API + `persona_list_team` 헌법 순서. Sec.1.2 v0.6.5 18명 기준 매핑 정합. T2 4팀 명단 헌법 순서(PL\|drafter\|reviewer\|finalizer) 전수 PASS. |
| 10 | shellcheck + 분량 임계 | 3 | shellcheck 4 file rc=0 + drafter 561줄(≤ 800) + reviewer 155줄(≤ 600) — 단, R-5 `_send_claude` eval / R-6 `_run` `eval "$@"` 패턴은 코드 스타일 차원 권고로 잔존. functional 영향 X이나 7점 감점. |
| **100** | **총점** | **90** | **임계 80% 통과** |

---

## Sec.2. Acceptance Criteria

| AC# | 내용 | 상태 | 근거 |
|----|------|------|------|
| AC-1 | 4-pane 구조 (PL 왼쪽 큰 + drafter/reviewer/finalizer 오른쪽 stack) | ✅ | dry-run: `split-window -h` 1회 + `-v` 2회 + `select-layout main-vertical` + `resize-pane -x 62%` 시퀀스 확인. |
| AC-2 | `set-option -p @persona '<페르소나>'` 4 pane (헌법 line 92) | ✅ | reviewer R-1 — dry-run에서 4 pane 전건 set-option 확인 (공기성/카더가든/최우영/현봉식). |
| AC-3 | `pane-border-format` 조건부 `#{?@persona,#{@persona},#{pane_title}}` | ✅ | reviewer R-2 — dry-run 시퀀스 확인 (claude CLI auto-rename 후에도 @persona 유지). |
| AC-4 | `select-pane -T '<페르소나> [<role>·<effort>]'` 호환 라벨 | ✅ | dry-run 4 pane 전건 라벨 확인 (예: `'공기성 [PL·high]'`). |
| AC-5 | claude `--model X` + `CLAUDE_PERSONA*` 환경변수 자동 주입 | ✅ | drafter spawn_team + start_claude_team 양쪽 정착. T4 미지정 → empty 가드 PASS. |
| AC-6 | `_resolve_persona` = `show-option @persona` 우선 + `pane_title` fallback | ✅ | reviewer R-3 — claude 재시작 후에도 페르소나 정확 추출. |
| AC-7 | 페르소나 18명 model/effort/role/team 누락 0 | ✅ | T1 PASS + T5 이형진 CEO 특수 처리 PASS. |
| AC-8 | 4 팀 명단 헌법 순서 (PL\|drafter\|reviewer\|finalizer) | ✅ | T2 plan/design/dev/dev-be/dev-fe 전건 PASS + 역할 순서 4팀 PASS. |
| AC-9 | dev = dev-be alias | ✅ | T3 PASS. |
| AC-10 | #4 schema v0.5 호환 (settings_require_v04 grace period) | ✅ | spawn_team line 178 — schema 0.5 통과 (#4 reviewer 인계). |

---

## Sec.3. 검증 Trail (실측, finalizer 재실행)

```
$ bash scripts/test_personas.sh
PASS T1 18명 페르소나 role/team/effort 누락 없음
PASS T2 plan/design/dev/dev-be/dev-fe 명단 헌법 순서 (5건)
PASS T2 plan/design/dev-be/dev-fe 역할 순서 (4건)
PASS T3 dev = dev-be alias
PASS T4 미지정 persona model/effort/team → empty (3건)
PASS T5 이형진 model=empty / role=ceo / team=exec (3건)
─────────────────────────────────────
SUMMARY: 17 pass / 0 fail

$ bash scripts/spawn_team.sh dev --session 99 --dry-run --no-claude  (요약)
DRY-RUN: tmux new-session -d -s '99' ...
DRY-RUN: tmux split-window -h / -v / -v                          (4-pane)
DRY-RUN: tmux select-layout main-vertical
DRY-RUN: tmux resize-pane -x 62%                                 (PL 큰 pane)
DRY-RUN: tmux set-option -p @persona '공기성/카더가든/최우영/현봉식'
DRY-RUN: tmux select-pane -T '공기성 [PL·high]' (외 3건)
DRY-RUN: tmux set-option pane-border-status top
DRY-RUN: tmux set-option pane-border-format ' #{?@persona,#{@persona},#{pane_title}} '
   (DRY-RUN — 실제 tmux 변경 없음)

$ shellcheck scripts/spawn_team.sh scripts/start_claude_team.sh scripts/test_personas.sh scripts/lib/personas.sh
rc=0  (4 file 전건 PASS)
```

→ **bridge_protocol Sec.4 헌법 표(line 92) 정합 + 운영자 명시 모델/effort 자동 주입 RESOLVED.**

---

## Sec.4. 결정 + 영역 외

### 4.1 결정
- **spawn_team.sh = 4-pane 헌법 + @persona 영구 식별 + 모델/effort 자동 주입 = 정착 단계 진입.**
- `lib/personas.sh` 신규 = 18명 페르소나 단일 source of truth (Sec.1.2 v0.6.5 기준).
- `start_claude_team.sh` _resolve_persona = show-option `@persona` 우선 → claude 재시작 면역.
- test_personas.sh = regression 방지 자산 (페르소나 인사 변경 시 T1/T2 재실행 필수).

### 4.2 영역 외 (다음 의제 / 다음 버전 위임)

| R# | 내용 | 위임 위치 |
|---|------|---------|
| R-5 | spawn_team `_send_claude` eval 패턴 — quote escape 복잡, 명령 배열 + `"$@"` 안전 | code style — v0.6.7+ 별도 PR |
| R-6 | spawn_team `_run` `eval "$@"` — DRY-RUN 디버깅 호환, SC2294 disable | code style — v0.6.7+ 별도 PR |
| 추가 권고 | switch_team.sh post-write neo 4 키 read-back (#4 reviewer R-5) | 본 의제 #5 영역에서 별도 처리 안 함 — drafter가 spawn_team rewrite 영역 한정 |

### 4.3 다음 단계 (Round B 종결 후)
- **PL S5 (CHANGELOG.md v0.6.6 항목 작성 + bridge 보고)** 진입 — 본 finalizer 영역 외.
- HANDOFF_v0.6.6.md Round B 완료 trail 갱신 = 별도 PL commit (본 finalizer commit 분리 — A 패턴 §6.3 reference only 정합).
- v0.6.6 릴리스 후 git push (v0.6.4 + v0.6.5 + v0.6.6 묶음, 이월 항목).

---

## Sec.5. 참조 (verbatim 흡수 X)

| SHA / 경로 | role | 영역 | 분량 |
|-----------|------|------|------|
| 2e1fd2b | drafter | spawn_team rewrite (110→309줄) + start_claude_team(89→124줄) + personas.sh(신규 128줄) | 3 file / 444 ins-82 del |
| 77ae46d | reviewer | R-1 @persona / R-2 border-format / R-3 _resolve_persona / R-4 17 테스트 / R-5/R-6 권고 | 3 file / 155 line |
| scripts/lib/personas.sh | lib | 18명 페르소나 single source of truth (Sec.1.2 v0.6.5) | 128줄 |
| scripts/test_personas.sh | asset | regression 방지 스모크 (read-only API) | 122줄 |
| docs/bridge_protocol.md Sec.4 | constitution | pane 식별 헌법 표 (line 92 @persona) — 1차 출처 | — |

> **헌법 정합:** 본 finalizer doc은 drafter/reviewer 본문을 verbatim 흡수하지 않고 SHA + verdict + Score + AC + trail + 결정만 기재. 사고 14 헌법(`bridge_protocol.md` Sec.6) + a_pattern_checklist §2.4 + §6.3 정합. 본 finalizer §6.3 8항목 자체 점검 통과.
