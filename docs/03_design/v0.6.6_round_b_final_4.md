---
version: v0.6.6
round: B
agenda: "#4"
topic: settings.json schema v0.5 (13→16 stage 정합 + 하위호환)
date: 2026-04-27
mode: Standard
status: final
authored_by: 현봉식 (개발팀 백엔드 파이널리즈, Sonnet/medium, Orc-066-dev:1.4)
upstream:
  - dispatch/2026-04-27_v0.6.6_infra_standard.md (의제 #4)
  - f5cc549 drafter(v0.6.6 #4) — schema v0.5 + helper 골격 (settings.json + lib/settings.sh + ai_step.sh)
  - 5ebc2fe reviewer(v0.6.6 #4) — R-1~R-6 (docstring + 12 테스트 영구화 + PL 검증 trail + 권고 3건)
session: v0.6.6 Round B
---

# v0.6.6 Round B — 의제 #4 마감 (settings.json schema v0.5)

> **상위:** drafter `f5cc549` → reviewer `5ebc2fe` (R-1~R-6)
> **본 문서:** Round B finalizer reference doc (verbatim 흡수 X — SHA chain만 인용, 헌법 §2.4 정합).
> **상태:** ✅ **PASS — Score 92% ≥ 80% → halt 조건(`ai_step.sh 기존 호환 FAIL`) 통과 / Round B 의제 #4 완료. 의제 #5 진입 게이트 OPEN.**

---

## Sec.0. Verdict + Halt 조건

| 항목 | 값 |
|------|------|
| **Verdict** | ✅ PASS |
| **Score** | 92% (≥ 80% 임계 충족) |
| **dispatch halt 조건** | "settings.json schema 변경 후 ai_step.sh 기존 호환 FAIL" → ❌ NOT TRIGGERED (12/12 PASS + --status PASS + switch_team --status PASS) |
| **분량 임계** | drafter 3 file/128 line (≤ 800줄) + reviewer 2 file/99 line (≤ 600줄) ✅ |
| **A 패턴 정합** | drafter / reviewer 분리 commit 2건 + R-N 마커 1~6 trail ✅ |

---

## Sec.1. Score 80% 임계 채점

| 가중치 | 항목 | 점수 | 근거 |
|---:|------|---:|------|
| 30 | 하위호환 + breaking change 최소화 | 30 | drafter neo 4 키(stage11~14) 추가 + legacy 4 키(stage8~11) 보존 + `_comment_legacy_v04` / `_comment_neo_v05` inline trail. settings_require_v04 grace period(0.4|0.5 허용) + settings_require_v05 strict 분리. |
| 25 | 실측 검증 trail | 24 | reviewer test_settings_v05_compat.sh 12 PASS + ai_step.sh --status schema 0.5 인식 + jq JSON valid + switch_team --status legacy lookup PASS. finalizer 재실행 시점 12/12 PASS 재확인. |
| 20 | compat helper 설계 | 19 | settings_read_stage_assign_compat (NEO 우선 + LEGACY fallback) + _ai_step_assign_key_for_16 (16-stage 마커 매핑) + _ai_step_read_assign_compat alias. T3 NEO 우선 + T4 LEGACY fallback + T5 둘 다 missing 가드 모두 PASS. |
| 15 | R-N 마커 trail (사고 14 헌법 정합) | 14 | reviewer commit body R-1 (docstring) + R-2 (테스트 영구화) + R-3 (PL 검증 직접 실행) + R-4~R-6 (권고 + 영역 외 위임) 각 마커 사유 명시. |
| 10 | 분량 + 영역 외 분리 | 5 | drafter 128 + reviewer 99 (≤ 800/600) — 단, R-4 (16-stage dispatcher 호출 분기 누락) = drafter helper 미사용 dangling. 의제 #5 또는 v0.6.7 위임 명시했으나 5점 감점. |
| **100** | **총점** | **92** | **임계 80% 통과** |

---

## Sec.2. Acceptance Criteria

| AC# | 내용 | 상태 | 근거 |
|----|------|------|------|
| AC-1 | `.claude/settings.json` schema_version 0.4 → 0.5 bump | ✅ | drafter f5cc549 + jq `"0.5"` 확인. |
| AC-2 | neo 4 키 추가 (stage11_impl / stage12_review / stage13_fix / stage14_verify) | ✅ | jq stage_assignments 8 키 inspection PASS. |
| AC-3 | legacy 4 키 보존 (하위호환) | ✅ | switch_team.sh --status legacy lookup PASS (rc=0). |
| AC-4 | settings_require_v04 grace period (0.4 OR 0.5 허용) | ✅ | T2 PASS (schema=0.5 허용). |
| AC-5 | settings_require_v05 strict 신규 (16-stage 의무 영역) | ✅ | T1 PASS (schema=0.5). |
| AC-6 | settings_read_stage_assign_compat (NEO 우선 + LEGACY fallback) | ✅ | T3 NEO 우선 + T4 LEGACY fallback + T5 둘 다 missing → empty 모두 PASS. |
| AC-7 | _ai_step_assign_key_for_16 helper (stage11~14 매핑) | ✅ | T6a~d PASS (stage11~14 4종) + T7 13-stage 회귀 PASS. |
| AC-8 | ai_step.sh --status에 schema_version + neo 4 키 표시 | ✅ | --status 출력에 `schema_version: 0.5` + legacy 4 + neo 4 키 모두 정상 표시. |

---

## Sec.3. 검증 Trail (실측, finalizer 재실행)

```
$ bash scripts/test_settings_v05_compat.sh
PASS T1 settings_require_v05 (schema=0.5)
PASS T2 settings_require_v04 grace (schema=0.5 허용)
PASS T3 compat NEO 우선 (stage11_impl=claude)
PASS T4 compat LEGACY fallback (stage9_review=codex)
PASS T5 compat both missing → empty
PASS T6a stage11→stage11_impl
PASS T6b stage12→stage12_review
PASS T6c stage13→stage13_fix
PASS T6d stage14→stage14_verify
PASS T7 13-stage 회귀 OK (stage8→stage8_impl)
PASS T8 ai_step compat alias (codex)
PASS T9 ai_step compat 인자 누락 가드 (empty)
─────────────────────────────────────
SUMMARY: 12 pass / 0 fail

$ bash scripts/ai_step.sh --status
  schema_version: 0.5
  stage_assignments (legacy 13-stage):
    stage8_impl: claude / stage9_review: codex / stage10_fix: claude / stage11_verify: claude
  stage_assignments (neo 16-stage, v0.5):
    stage11_impl: claude / stage12_review: codex / stage13_fix: claude / stage14_verify: claude

$ jq -e '.schema_version' .claude/settings.json
"0.5"  → JSON valid + 8 키 정합
```

→ **dispatch halt 조건 (Sec.10 §2) "settings.json schema 변경 후 ai_step.sh 기존 호환 FAIL → 📡 WAITING" = NOT TRIGGERED.**

---

## Sec.4. 결정 + 영역 외

### 4.1 결정
- **schema v0.5 정착 = legacy 4 키 + neo 4 키 = 8 키 양립 (`_comment_legacy_v04` / `_comment_neo_v05` 인라인 trail).**
- compat helper 3종(`settings_read_stage_assign_compat` / `_ai_step_assign_key_for_16` / `_ai_step_read_assign_compat`) 정착 → 16-stage flow 진입 가능 base 확보.
- grace period(`settings_require_v04`가 0.4|0.5 모두 허용) → v0.7 rename 시점까지 호환 보호.
- test_settings_v05_compat.sh 신규 = regression 방지 자산 (v0.7+ schema 변경 시 재실행 필수).

### 4.2 영역 외 (다음 의제 / 다음 버전 위임)

| R# | 내용 | 위임 위치 |
|---|------|---------|
| R-4 | `_ai_step_assign_key_for_16` dispatcher 호출 분기 누락 (helper만 정의, 미사용) | v0.6.7 또는 별도 의제 — 16-stage flow 실제 사용 시점에 dispatch 마커(stage11~14) → helper 라우팅 추가 |
| R-5 | `switch_team.sh` post-write neo 4 키 read-back 검증 누락 | 의제 #5 (spawn_team.sh 보강) 영역에서 함께 정합 검토 |
| R-6 | `_asp_n11~n14` display 변수 = compat helper 미사용 (display only) | code review 차원, functional 영향 X — 별도 PR 후보 |

### 4.3 다음 단계
- **의제 #5 (spawn_team.sh split panes 보강)** 진입 — drafter 단계.
- 의제 #5 drafter는 본 finalizer §4.2 R-5 (neo 4 키 read-back 검증)도 함께 정합 검토 권고.
- HANDOFF_v0.6.6.md 추가 항목: `start_claude_team.sh` + `spawn_team.sh` 모델/effort 자동 주입(현재 미전달, v0.6.6 릴리스 전 해결 필수) — 운영자 명시.

---

## Sec.5. 참조 (verbatim 흡수 X)

| SHA / 경로 | role | 영역 | 분량 |
|-----------|------|------|------|
| f5cc549 | drafter | settings.json schema v0.5 + helper 골격 (settings.json + lib/settings.sh + ai_step.sh) | 3 file / 128 line |
| 5ebc2fe | reviewer | R-1 docstring + R-2 12 테스트 영구화 + R-3 PL 검증 trail + R-4~R-6 권고 | 2 file / 99 line |
| scripts/test_settings_v05_compat.sh | asset | regression 방지 스모크 (read-only, settings.json 미변경) | 90줄 |
| scripts/lib/settings.sh | lib | settings_require_v04 grace + settings_require_v05 strict + read_compat | 11 line touched |

> **헌법 정합:** 본 finalizer doc은 drafter/reviewer 본문을 verbatim 흡수하지 않고 SHA + verdict + Score + AC + trail + 결정만 기재. 사고 14 헌법(`bridge_protocol.md` Sec.6) + a_pattern_checklist §2.4 + §6.3 정합. 본 finalizer §6.3 8항목 자체 점검 통과.
