---
version: v0.6.4
stage: 5 (technical_design)
date: 2026-04-27
mode: Strict
has_ui: yes
risk_level: medium
status: drafter_v1
session: 28 (v0.6.4 Stage 5)
authored_by: 카더가든 (개발팀 백앤드 주임연구원, drafter, Haiku/medium)
team_session: Orc-064-dev (4 panes — 공기성 PL / 카더가든 drafter / 최우영 reviewer / 현봉식 finalizer)
upstream:
  - docs/02_planning_v0.6.4/planning_index.md (Stage 4.5 게이트 PASS, Score 99/100, commit 7c139b6)
  - docs/02_planning_v0.6.4/planning_01_M1_scaffold.md (272줄)
  - docs/02_planning_v0.6.4/planning_02_M2_data.md (472줄)
  - docs/02_planning_v0.6.4/planning_03_M3_render.md (436줄)
  - docs/02_planning_v0.6.4/planning_04_M4_pending_notif.md (422줄)
  - docs/02_planning_v0.6.4/planning_05_M5_windows_personas.md (537줄, F-D3 final 박힘)
  - docs/01_brainstorm_v0.6.4/brainstorm.md (Stage 1, 의제 1~8)
  - dispatch/2026-04-27_v0.6.4_stage5_technical_design.md (Stage 5 dispatch)
absorbs_operator_decisions:
  - Q1 = F-D3 final (18명 박스 + PM 스티브 리 별도 상단 status bar 1행 = 19명 표시 / CTO·CEO 미표시)
  - Q2 = Q-M2-1 (정확 hook 채택, Stage 8 +8h 인정)
  - Q3 = Q-M4-2 (Pushover 회피 / osascript 기본 / Windows = plyer or win10toast)
  - Q4 = Q-M5-2 (P1 유지 — macOS 단독 + Windows skeleton)
  - Q5 = Q-M2-5 (idle 통합 confirm — tmux 미존재 = idle)
absorbs_policy_commits:
  - F-D1 본문 (PersonaState 단일 spec + PersonaDataCollector / PendingDataCollector SRP 분리)
  - F-D2 본문 (scripts/dashboard.py 단일 진입 + scripts/dashboard/<module>.py 패키지)
  - F-D4 본문 (sync/async 결정 — Threaded sync wrapper 채택)
---

# v0.6.4 Stage 5 — Technical Design (drafter v1 초안)

> **본 문서:** `docs/03_design/v0.6.4_technical_design.md` (Stage 5 기술 설계 v1 초안, 카더가든 drafter)
> **상위:** `docs/02_planning_v0.6.4/planning_index.md` (Stage 4.5 게이트 PASS, 통합 Stage 5 이월 14건 단일 source)
> **다음:** Stage 5 design_review (최우영 be-reviewer) → Stage 5 design_final (현봉식 be-finalizer) → Stage 6/7 디자인팀 첫 등판 (Orc-064-design)
> **모드 / has_ui / risk:** Strict / yes / medium — 첫 실전 프로젝트 + 디자인팀 첫 등판 + 운영자 일상 도구
> **상태:** 🟡 drafter v1 초안 (reviewer 보강 권고 영역 = R-N 마커 reviewer 영역)

본 v1 초안은 드래프터 영역에서 dispatch Sec.핵심작업 A~G + 자율 영역(M1~M5 모듈 경계 / 데이터 흐름 / 에러 경로 / 테스트 / 보안 / F-D 본문 결정 / Q1~Q5 흡수)을 박은 산출물입니다. drafter 권한 밖(통합 verdict / Score 최종 가중치 분배 / commit 분리)은 finalizer 영역으로 표시만 박습니다.

---

## Sec. 0. 요약 (drafter v1)

### Sec. 0.1 본 v1 초안 핵심 결정 박음

| ID | 영역 | 핵심 결정 (본 stage 본문) |
|----|------|------------------------|
| **F-D1** | 데이터 모델 단일 spec | `PersonaState` 6필드 dataclass + `PersonaDataCollector` (M2) + `PendingDataCollector` (M4) SRP 분리. 직렬화 = `dataclasses.asdict()` 기준. 등가 비교 = `frozen=False + eq=True`. Sec.3 본문. |
| **F-D2** | 산출물 위치 단일화 | 진입점 `scripts/dashboard.py` (≤ 80줄, App + 부트). 모듈 패키지 `scripts/dashboard/` 11개 파일. 슬래시 정의 `.claude/commands/dashboard.md`. Sec.2 본문. |
| **F-D4** | sync vs async 결정 | **Threaded sync wrapper 채택** — `PersonaDataCollector` / `PendingDataCollector`는 sync `def`로 시작, textual `App.run_worker(thread=True)` worker가 1~2초 polling 호출. async/await 미채택. 근거 = Sec.5 본문. |
| **Q1 흡수** | F-D3 final 19명 표시 | 박스 3개 (기획/디자인/개발) 18명 + 상단 `Header` status bar 1행에 PM 스티브 리 1명 = 19명. CTO·CEO 미표시. status bar = textual `Header` 위젯 확장 + 1행. Sec.11.1 본문. |
| **Q2 흡수** | 정확 hook 채택 | `~/.claude/hooks/session-end.sh` (외부 hook 스크립트) + `.claude/dashboard_state/<session_id>.json` 파일 기반 토큰량 정확 인입. Stage 8 +8h 비용 인정. fallback = capture-pane regex 추정 ~95%. Sec.7.3 본문. |
| **Q3 흡수** | osascript 기본 | macOS = `osascript -e 'display notification "<msg>" with title "jOneFlow" sound name "Submarine"'` subprocess.run 호출. dedupe TTL 5분 (`functools` LRU 기반). Windows fallback = `plyer` 0순위 / `win10toast` 1순위 비교. Pushover / CCNotify 회피. Sec.9.3 본문. |
| **Q4 흡수** | P1 유지 | v0.6.4 = macOS 단독 본 가동 + Windows skeleton(`scripts/dashboard/platform_compat.py` placeholder). 본 가동 검증 = v0.6.5+ 이월. Sec.10.1 본문. |
| **Q5 흡수** | idle 통합 confirm | tmux 세션 미존재 = `status="idle"` 통합. offline 분리 옵션 0건. `PersonaDataCollector.fetch_all_personas()`가 미존재 페르소나도 `idle`로 yield. Sec.7.2 본문. |

### Sec. 0.2 본 stage 산출 통계 (drafter 추정 — finalizer 확정)

- **본문 분량:** ~1,100줄 (drafter v1)
- **AC 총합:** 31건 (자동 22 / 수동 4 / Stage 8 이월 5) — 자동 비율 = 22/26 ≈ **85%** (planning_index 73% 상향). Stage 8 이월은 자동/수동 분모 외.
- **F-D 본문 결정:** 3건 (F-D1 / F-D2 / F-D4)
- **운영자 결정 흡수:** 5/5 (Q1~Q5)
- **Stage 5 이월 14건 처리:** 9건 본문 결정 + 5건 디자인팀 boundary
- **디자인팀 boundary slot:** 4건 (색상 토큰 / 박스 border / 진행률 바 / 스파크라인)

본 통계는 drafter v1 추정값입니다. finalizer 통합 + reviewer 보강 후 design_final.md에서 확정 박힘 영역으로 보입니다.

---

## Sec. 1. 목적 + 본 stage 범위

### 1.1 목적

본 Stage 5 기술 설계는 v0.6.4 운영자 대시보드의 **모듈 경계 / 데이터 흐름 / 에러 경로 / 테스트 / 보안**을 확정합니다. Stage 4 plan_final 5건(M1~M5, 2,151줄) + planning_index(330줄)에서 박힌 정책 commit 4건(F-D1~F-D4) 본문 후속 결정을 흡수하고, 운영자 결정 5건(Q1~Q5)을 인터페이스 spec 형식으로 본문에 박습니다. Stage 8 구현이 본 문서를 단일 source-of-truth로 따라갈 수 있도록 모든 결정 영역을 닫는 것이 본 stage의 책임입니다.

### 1.2 본 stage 범위 (확정 영역)

- ✅ M1~M5 모듈 패키지 디렉토리 구조 (F-D2 본문)
- ✅ PersonaState / PendingPush / PendingQuestion dataclass 6/6/6 필드 spec (F-D1 본문)
- ✅ Collector / Renderer / Notifier 인터페이스 시그니처 sync 통일 (F-D4 본문)
- ✅ 토큰량 정확 hook 인터페이스 + 폴백 regex (Q2)
- ✅ macOS osascript 알림 명령 + Windows fallback 비교 (Q3)
- ✅ 19명 표시 layout (status bar 1행) (Q1)
- ✅ idle 통합 정책 + tmux 미존재 처리 (Q5)
- ✅ Windows P1 skeleton 인터페이스 (Q4)
- ✅ AC 명세 31건 (자동 22 / 수동 4 / Stage 8 이월 5)
- ✅ 에러 경로 / 테스트 전략 / 보안 정책 본문

### 1.3 범위 밖 (Stage 5 이월 → 본 stage 처리 내역 Sec.12 단일 source)

- 색상 토큰 색값 (Stage 6 디자인팀 영역 — 본 stage = 토큰명 슬롯만)
- 박스 border / margin / padding 픽셀 (Stage 6 디자인팀)
- 진행률 바 / 스파크라인 시안 (Stage 6 디자인팀)
- 6~9행 visual 검증 시나리오 (Stage 5 → Stage 6 협업 영역)
- Windows 본 가동 검증 (Q4 = P1 유지 → v0.6.5+ 이월)
- 18명 페르소나별 데이터 수집 detail 매핑 (Stage 8 진입 직전 = personas_18.md blocking 시점, F-X-6)
- Pending Q deep-link (v0.6.5+ 영구 이월, brainstorm 의제 5)

---

## Sec. 2. 모듈 패키지 구조 (F-D2 본문 결정)

### 2.1 디렉토리 트리 (drafter 자율 영역 — 본 stage 본문 결정)

```
jOneFlow/
├── scripts/
│   ├── dashboard.py                      ← 단일 진입점 (≤ 80줄, F-D2)
│   └── dashboard/                        ← 모듈 패키지 (F-D2)
│       ├── __init__.py                   ← 빈 모듈 (3줄)
│       ├── models.py                     ← PersonaState / PendingPush / PendingQuestion (~80줄)
│       ├── persona_collector.py          ← PersonaDataCollector (M2 / F-D1) (~200줄)
│       ├── pending_collector.py          ← PendingDataCollector (M4 / F-D1) (~150줄)
│       ├── tmux_adapter.py               ← tmux capture-pane 추출 (~120줄)
│       ├── token_hook.py                 ← claude CLI hook 인입 (Q2 정확) (~80줄)
│       ├── team_renderer.py              ← TeamRenderer (M3) (~150줄)
│       ├── pending_widgets.py            ← PendingPushBox / PendingQBox (M4) (~120줄)
│       ├── status_bar.py                 ← PMStatusBar (Q1 / F-D3) (~60줄)
│       ├── notifier.py                   ← Notifier ABC + 3채널 (M4 / Q3) (~120줄)
│       ├── platform_compat.py            ← Windows skeleton (Q4 / M5) (~50줄)
│       └── tests/                        ← 단위 테스트 (Stage 8 작성, Stage 9 Opus 리뷰)
│           ├── __init__.py
│           ├── test_models.py
│           ├── test_persona_collector.py
│           ├── test_pending_collector.py
│           ├── test_team_renderer.py
│           ├── test_notifier.py
│           └── test_dedupe.py
├── .claude/
│   ├── commands/
│   │   └── dashboard.md                  ← /dashboard 슬래시 정의 (~30줄)
│   ├── hooks/                            ← Q2 정확 hook (Stage 8 신규)
│   │   └── session-end.sh                ← claude CLI 종료 시 토큰량 dump
│   └── dashboard_state/                  ← Q2 hook 출력 (Stage 8 신규, gitignore)
│       └── <session_id>.json
└── requirements.txt                      ← textual >= 0.50.0 (M1)
```

### 2.2 파일별 책임 표

| 파일 | 책임 | 외부 의존 | M | F-D |
|------|------|---------|---|-----|
| `scripts/dashboard.py` | 단일 진입점 — `DashboardApp(App)` 클래스 + `compose()` + `on_mount()` | textual | M1 | F-D2 |
| `scripts/dashboard/models.py` | dataclass 3종 (PersonaState / PendingPush / PendingQuestion) | dataclasses, datetime | 횡단 | F-D1 |
| `scripts/dashboard/persona_collector.py` | M2 — `PersonaDataCollector` (페르소나 상태 sync polling) | tmux_adapter, token_hook | M2 | F-D1 |
| `scripts/dashboard/pending_collector.py` | M4 — `PendingDataCollector` (Pending Push/Q 추출) | tmux_adapter, git, dispatch md | M4 | F-D1 |
| `scripts/dashboard/tmux_adapter.py` | tmux 세션 목록 + capture-pane 호출 + regex | subprocess | M2 / M4 | — |
| `scripts/dashboard/token_hook.py` | `.claude/dashboard_state/*.json` 인입 + capture-pane fallback | json, glob | M2 | Q2 |
| `scripts/dashboard/team_renderer.py` | M3 — `TeamRenderer(teams_data) -> Widget` + 다중 버전 sub-row `└` | textual.widgets | M3 | F-D1 |
| `scripts/dashboard/pending_widgets.py` | M4 — `PendingPushBox` / `PendingQBox` textual 위젯 | textual.widgets | M4 | F-D1 |
| `scripts/dashboard/status_bar.py` | Q1 / F-D3 — PM 스티브 리 status bar 1행 (Header 확장) | textual.widgets | M5 / Q1 | F-D3 |
| `scripts/dashboard/notifier.py` | Notifier ABC + OSAScriptNotifier + WindowsNotifier(stub) + dedupe | abc, subprocess, functools | M4 | Q3 |
| `scripts/dashboard/platform_compat.py` | macOS / Linux / Windows feature detection + skeleton | sys, platform | M5 | Q4 |
| `.claude/commands/dashboard.md` | `/dashboard` 슬래시 정의 + python3 호출 라인 | — | M1 | F-D2 |
| `.claude/hooks/session-end.sh` | claude CLI 종료 시 토큰량 dump → JSON | bash, jq | M2 | Q2 |

### 2.3 충돌 해소 trail

| v1 후보 (Stage 4 plan_final) | 본 stage 결정 |
|----------|-----------|
| ~~`src/dashboard/components/...`~~ (M3 v1) | 회수 — `scripts/dashboard/team_renderer.py` 단일 |
| ~~`dashboard/...`~~ (M4 v1) | 회수 — `scripts/dashboard/` 패키지 단일 |
| ~~`scripts/dashboard.py` 또는 `dashboard/main.py`~~ (M1 v1) | 회수 — `scripts/dashboard.py` 단일 진입점 |
| ~~별도 `requirements-dashboard.txt`~~ (M1 v1) | 회수 — `requirements.txt` 단일 (jOneFlow 컨벤션) |

본 결정은 plan_final F-D2 본문(planning_index Sec.4) verbatim 흡수입니다. drafter 임의 재정의 0건입니다.

---

## Sec. 3. 데이터 모델 (F-D1 본문 결정)

### 3.1 PersonaState dataclass 본문 (drafter 자율 영역)

```python
# scripts/dashboard/models.py

from dataclasses import dataclass, field, asdict
from datetime import datetime
from typing import Literal, Optional


PersonaStatus = Literal["working", "idle"]
"""Q5 idle 통합 confirm — tmux 세션 미존재 = idle 통합 (offline 분리 옵션 0건)."""


@dataclass(eq=True, frozen=False)
class PersonaState:
    """페르소나 실시간 상태 — F-D1 본문 결정, M2/M3/M4 횡단 단일 spec.

    필드 선택 근거:
        - name: 정규 페르소나명 (operating_manual.md Sec.1.2 18명 정의 그대로).
        - team: "기획" / "디자인" / "개발" — F-D3 박스 3개와 1:1 매핑.
        - status: "working" / "idle" 2단계 — brainstorm 의제 3 + Q5 idle 통합.
        - task: working일 때만 채워짐. idle일 때는 None (Optional).
        - tokens_k: float, k 단위. 누적 (input + output) — Q2 정확 hook 인입.
        - last_update: ISO 8601 — 데이터 신선도 표시용.
    """

    name: str
    team: Literal["기획", "디자인", "개발"]
    status: PersonaStatus
    task: Optional[str]
    tokens_k: float
    last_update: datetime

    def to_json(self) -> dict:
        """직렬화 — dataclasses.asdict() 기준 + datetime.isoformat() 변환."""
        d = asdict(self)
        d["last_update"] = self.last_update.isoformat()
        return d
```

**필드 선택 근거 (drafter 자율 본문):**
- `team` 한국어 Literal 타입 — F-D3 final layout(`기획팀` / `디자인팀` / `개발팀`)과 1:1 정합. 영문 "planning"/"design"/"dev" 변환 어댑터 = M3 `team_renderer.py` 내부 영역 (외부 spec 노출 X).
- `task` Optional — Q5 idle 통합 confirm 후 idle 페르소나는 task 미보유. M3 렌더링은 `task is None` 분기로 행 2~3을 생략합니다.
- `tokens_k: float` — `%.1fk` 표시 정밀도(M3 F-M3-3 흡수)와 정합. int 채택 시 소수점 1자리 손실.
- `last_update: datetime` — ISO 8601 직렬화. textual 갱신 시 `last_update` ≥ 2초 전이면 stale 표시 영역(드래프터 자율 → reviewer 보강 권고 R-1).

### 3.2 PendingPush / PendingQuestion dataclass 본문

```python
@dataclass(eq=True, frozen=False)
class PendingPush:
    """M4 — 운영자 승인 대기 push/commit (read-only 표시 only)."""

    item_id: str           # "push-001", "commit-002"
    item_type: Literal["push", "commit"]
    description: str       # "v0.6.4 Stage 2 planning commit"
    timestamp: datetime
    initiator: str         # 페르소나 이름
    severity: Literal["high", "medium", "low"]


@dataclass(eq=True, frozen=False)
class PendingQuestion:
    """M4 — 운영자 결정 영역 큐 (read-only 표시, 응답은 회의창 흐름 유지)."""

    q_id: str              # "Q1", "Q5", "Q-NEW-1"
    category: Literal["scope", "decision", "risk", "approval"]
    description: str
    source: str            # "dispatch/..._planning.md"
    timestamp: datetime
    priority: Literal["critical", "high", "medium", "low"]

    def dedupe_key(self) -> str:
        """알림 dedupe 5분 TTL용 해시 키 (Q3 / F-X-7-#3 흡수)."""
        return f"{self.q_id}:{self.timestamp.replace(second=0, microsecond=0).isoformat()}"
```

`dedupe_key()` 메서드는 5분 단위(`replace(second=0, microsecond=0)` + 분 단위 timestamp)로 해시를 안정화하여, 같은 분 내 반복 캐치 시 동일 키를 생성합니다. `Notifier.notify()` 측 LRU 캐시(`maxsize=128`)와 정합합니다(Sec.9.3).

### 3.3 단일 spec 효과 (F-D1 본문)

| 영역 | 본 stage 결정 |
|------|----------|
| **M3 `TeamRenderer.render()` 입력** | `Dict[str, List[PersonaState]]` (key = "기획"/"디자인"/"개발"). M3 v1의 별도 `TeamMember` dataclass 영구 회수. |
| **M2 ↔ M4 SRP 분리** | `PersonaDataCollector` ≠ `PendingDataCollector`. 두 클래스가 같은 `tmux_adapter` 모듈을 의존하지만 서로의 메서드는 호출하지 않음 (단방향). |
| **직렬화 형식** | `to_json()` = `dataclasses.asdict()` + `datetime.isoformat()`. JSON dump 가능 영역 명시 — Stage 8 테스트 fixture 직렬화에 사용. |
| **등가 비교** | `frozen=False, eq=True` — 단위 테스트에서 expected vs actual 비교 가능. unhashable이라 set 멤버십 0건 (대신 `name` 기반 dict 키). |

---

## Sec. 4. 데이터 흐름 (모듈 경계 + sync 단방향)

### 4.1 흐름 다이어그램

```
┌──────────────────────────────────────────────────────────────────┐
│                  textual App (scripts/dashboard.py)              │
│                          DashboardApp                            │
│                                                                  │
│   on_mount()  ──► run_worker(thread=True, refresh_loop)          │
│                                                                  │
│         (1~2초 polling, F-D4 sync wrapper, Sec.5)               │
│                          ▼                                       │
│   ┌──────────────────────────────────────────────────┐           │
│   │  refresh_loop() — sync polling worker            │           │
│   └─────┬───────────────────────────────────────┬────┘           │
│         ▼                                       ▼                │
│   ┌──────────────────┐                  ┌──────────────────┐     │
│   │ PersonaCollector │                  │ PendingCollector │     │
│   │   .fetch_all()   │                  │ .get_pushes()    │     │
│   │                  │                  │ .get_questions() │     │
│   └────┬─────────────┘                  └─────┬────────────┘     │
│        │                                       │                 │
│        ▼ (List[PersonaState])                  ▼ (List[Pending*]) │
│   ┌──────────────────┐                  ┌──────────────────┐     │
│   │  TeamRenderer    │                  │ PendingPushBox   │     │
│   │  StatusBar (PM)  │                  │ PendingQBox      │     │
│   └──────────────────┘                  └─────┬────────────┘     │
│                                                │                 │
│                                                ▼ 신규 Q 도착시   │
│                                          ┌──────────────────┐    │
│                                          │   Notifier       │    │
│                                          │ (osascript /     │    │
│                                          │  Windows fallbk) │    │
│                                          └──────────────────┘    │
└──────────────────────────────────────────────────────────────────┘

         ▲                                    ▲
         │ tmux capture-pane                  │ git status / dispatch md regex
         │ (PersonaCollector 의존)            │ (PendingCollector 의존)
         │                                    │
   ┌──────────────────────────────────────────────────────────────┐
   │  외부: tmux 세션 (bridge-* / Orc-*-plan/design/dev) +         │
   │        .claude/dashboard_state/*.json (Q2 hook) +             │
   │        git status / dispatch md 파일                          │
   └──────────────────────────────────────────────────────────────┘
```

### 4.2 모듈 경계 4원칙 (drafter 자율 본문)

1. **단방향 의존 (모듈 경계 #1):** UI(`team_renderer.py` / `pending_widgets.py` / `status_bar.py`) → Collector(`persona_collector.py` / `pending_collector.py`) → Adapter(`tmux_adapter.py` / `token_hook.py`) → 외부(tmux subprocess, file IO). 역방향 import 0건. UI 위젯이 Collector 내부 구현을 모르며, Collector가 textual import 0건.
2. **read-only 단방향 (#2):** 모든 Collector / Renderer / Notifier 출력은 표시·알림 only. write 명령(git push / git commit / file open with `'w'` mode) 0건. AC 자동 검증 = `grep -E "git push|git commit|open\(.*['\"]w['\"]"` 비-zero 시 FAIL.
3. **dataclass 횡단 단일 spec (#3):** PersonaState / PendingPush / PendingQuestion만 모듈 경계를 가로지름. primitive(str/int/dict) 직접 노출 0건. M3 `team_renderer.py`가 `tokens_k`를 dict로 받으면 위반.
4. **subprocess 격리 (#4):** tmux / osascript / claude CLI hook 외부 호출은 `tmux_adapter.py` / `notifier.py` / `token_hook.py` 3개 모듈에만 거주. 다른 모듈에서 `subprocess` import 시 위반 (테스트 자동 검증).

### 4.3 모듈 경계 4원칙 위반 자동 검증

| 원칙 | grep 명령 | 기대값 |
|------|----------|--------|
| #1 단방향 | `grep -lE "from .*team_renderer\|from .*pending_widgets" scripts/dashboard/{persona,pending,tmux}_*.py` | 0건 |
| #2 read-only | `grep -cE "git push\|git commit\|open\(.*['\"]w['\"]" scripts/dashboard/*.py` | 0건 |
| #3 dataclass 단일 | `grep -cE "@dataclass" scripts/dashboard/*.py \| awk -F: '{s+=$2} END{print s}'` | = 3 (models.py 한 곳에서만) |
| #4 subprocess 격리 | `grep -lE "import subprocess" scripts/dashboard/*.py` | = `tmux_adapter.py` / `notifier.py` / `token_hook.py` 3건 |

본 4원칙은 AC-T-1 ~ AC-T-4 4건으로 Sec.17 AC 표에 박았습니다.

---

## Sec. 5. F-D4 본문 결정 — Threaded sync wrapper 채택

### 5.1 채택 결정

본 v0.6.4 데이터 수집·렌더링 인터페이스는 **Threaded sync wrapper** 방식을 채택합니다.

- 모든 `Collector` 메서드는 sync `def`로 정의합니다 (`async def` 0건).
- `DashboardApp.on_mount()`에서 `self.run_worker(self._refresh_loop, thread=True)`로 백그라운드 sync 워커 실행.
- 워커가 1초마다 (Sec.7.4 갱신 주기) `PersonaDataCollector.fetch_all_personas()` + `PendingDataCollector.get_pending_*()`를 sync 호출 → `self.call_from_thread(self._update_widgets, ...)`로 메인 thread에 결과 push.

### 5.2 채택 근거 (drafter 자율 trade-off 본문)

| 옵션 | 장점 | 단점 | 채택 여부 |
|------|------|------|---------|
| **A. async/await 전면 채택** | textual 권장 패턴 일치 / I/O blocking 회피 | tmux subprocess는 sync 호출이라 `asyncio.to_thread` 필수 / 학습 비용 / Stage 8 구현 +6h | ❌ 회수 |
| **B. sync 메인 thread blocking** | 가장 단순 | textual 이벤트 루프 정지 (1~2초 freeze) — 사용자 가시 정지 발생 | ❌ 회수 |
| **C. Threaded sync wrapper (채택)** | sync 인터페이스 유지 / 메인 thread 비차단 / textual `run_worker(thread=True)` 표준 패턴 / Stage 8 구현 +0h | thread-safety 주의 (call_from_thread 박음) | ✅ 채택 |

**채택 근거 핵심 3건:**
1. M2 `PersonaDataCollector` / M4 `PendingDataCollector`의 외부 의존(tmux subprocess / file IO)은 **본질적으로 sync**입니다. `subprocess.run()` / `open().read()`는 `asyncio.to_thread` 래핑이 필요해서 옵션 A는 실질적으로 옵션 C와 동일한 thread 사용을 강요합니다. 인터페이스만 async로 바뀔 뿐 실행 모델은 동일합니다.
2. textual 0.50+에서 `App.run_worker(thread=True)`는 표준 패턴으로 안정화되어 있으며, Stage 4 plan_final F-D4 stub("sync 시작 / async 채택은 Stage 5")의 자연스러운 계승입니다.
3. M1 `on_mount()` 동기 훅과 정합성이 그대로 유지됩니다. M2 v1의 `persona_by_name`만 동기였던 일관성 깨짐은 sync 전면 통일로 해소됩니다.

### 5.3 thread-safety 영구 정책 (Stage 8 강제)

```python
# scripts/dashboard.py (drafter 의사 코드)

class DashboardApp(App):
    async def on_mount(self) -> None:
        self.persona_collector = PersonaDataCollector()
        self.pending_collector = PendingDataCollector()
        self.notifier = OSAScriptNotifier()
        self.run_worker(self._refresh_loop, thread=True, exclusive=True)

    def _refresh_loop(self) -> None:
        """Worker thread — 1초 polling sync."""
        while not self._exit_signal.is_set():
            try:
                personas = self.persona_collector.fetch_all_personas()
                pushes = self.pending_collector.get_pending_pushes()
                questions = self.pending_collector.get_pending_questions()
            except Exception as exc:
                # Sec.14 에러 경로 — staleness 표시로 폴백
                self.call_from_thread(self._show_stale, exc)
            else:
                self.call_from_thread(self._update_widgets, personas, pushes, questions)
            time.sleep(1.0)
```

`call_from_thread` 박음이 thread-safety 영구 정책입니다. Worker thread에서 textual 위젯을 직접 변경하면 race condition이 발생할 수 있어 Stage 9 코드 리뷰 자동 검증 영역(Sec.15.4)에 포함합니다.

---

## Sec. 6. M1 — scaffold technical detail

### 6.1 `scripts/dashboard.py` 본문 spec (drafter 자율)

| 영역 | 결정 |
|------|------|
| **App 클래스** | `class DashboardApp(App)` — textual 0.50+ 표준 |
| **종료 키** | `BINDINGS = [Binding("q", "quit", "Quit")]` — AC-M1-3 자동 검증 정합 |
| **compose() 구조** | `yield PMStatusBar()` (Q1 / status bar 1행) → `yield Horizontal(PlanningBox, DesignBox, DevBox)` → `yield Horizontal(PendingPushBox, PendingQBox)` |
| **on_mount()** | Collector / Notifier 초기화 + `run_worker(thread=True)` 워커 실행 |
| **종료 동작** | `action_quit()` → `self.exit()` |
| **textual 버전** | `>= 0.50.0` (M1 plan_final 그대로) |

### 6.2 `.claude/commands/dashboard.md` slash 정의

```markdown
---
description: jOneFlow 운영자 대시보드 (textual TUI)
allowed-tools: Bash
---

# /dashboard

jOneFlow AI팀 18명 + PM 1명 = 19명 실시간 상태 가시화.

```bash
$ARGUMENTS
exec venv/bin/python3 scripts/dashboard.py
```

종료 = `q`.
```

### 6.3 `requirements.txt` 추가 항목

```
textual>=0.50.0
```

`textual` 0.50+는 `App.run_worker(thread=True)` API 안정화 버전으로, F-D4 Threaded sync wrapper 정합성을 보장합니다. Pushover / plyer / win10toast는 본 v0.6.4 macOS 단독 본 가동에서는 미설치(Q3 / Q4 운영자 결정).

### 6.4 `scripts/init_project.sh` scaffold 통합 (M1 F-M1-3 / AC-M1-7)

```bash
# 본 v0.6.4 신규 추가 영역 (drafter 자율 의사 코드)

cp -r "$ROOT/scripts/dashboard.py" "$NEW_ROOT/scripts/dashboard.py"
cp -r "$ROOT/scripts/dashboard/" "$NEW_ROOT/scripts/dashboard/"
cp "$ROOT/.claude/commands/dashboard.md" "$NEW_ROOT/.claude/commands/dashboard.md"
```

---

## Sec. 7. M2 — 데이터 수집 layer technical detail

### 7.1 `PersonaDataCollector` 인터페이스 본문

```python
# scripts/dashboard/persona_collector.py

from typing import List, Optional
from datetime import datetime
from .models import PersonaState
from .tmux_adapter import TmuxAdapter
from .token_hook import TokenHook


class PersonaDataCollector:
    """M2 — 페르소나 상태 sync polling. F-D1 본문 단일 source.

    sync 인터페이스 (F-D4 Threaded sync wrapper 채택 — Sec.5).
    """

    PERSONAS_18 = [
        # 기획팀 4
        ("박지영", "기획"), ("김민교", "기획"), ("안영이", "기획"), ("장그래", "기획"),
        # 디자인팀 4
        ("우상호", "디자인"), ("이수지", "디자인"), ("오해원", "디자인"), ("장원영", "디자인"),
        # 개발팀 7
        ("공기성", "개발"), ("최우영", "개발"), ("현봉식", "개발"), ("카더가든", "개발"),
        ("백강혁", "개발"), ("김원훈", "개발"), ("지예은", "개발"),
    ]

    def __init__(self, tmux: Optional[TmuxAdapter] = None,
                 token_hook: Optional[TokenHook] = None) -> None:
        self.tmux = tmux or TmuxAdapter()
        self.token_hook = token_hook or TokenHook()

    def fetch_all_personas(self) -> List[PersonaState]:
        """18명 모든 페르소나 상태 반환 — tmux 미존재 페르소나도 idle로 yield (Q5)."""
        out: List[PersonaState] = []
        active_sessions = self.tmux.list_sessions()  # ["bridge-064", "Orc-064-dev", ...]
        for name, team in self.PERSONAS_18:
            state = self._infer_state(name, team, active_sessions)
            out.append(state)
        return out

    def fetch_team(self, team: str) -> List[PersonaState]:
        return [p for p in self.fetch_all_personas() if p.team == team]

    def persona_by_name(self, name: str) -> Optional[PersonaState]:
        for p in self.fetch_all_personas():
            if p.name == name:
                return p
        return None

    def _infer_state(self, name: str, team: str, sessions: List[str]) -> PersonaState:
        """drafter 자율 — 옵션 A capture-pane + Q2 token_hook fallback 결합."""
        ...  # Sec.7.2 본문
```

### 7.2 상태 추론 알고리즘 (Q5 idle 통합 confirm 본문)

```python
def _infer_state(self, name: str, team: str, sessions: List[str]) -> PersonaState:
    """Q5 idle 통합 confirm — tmux 세션 미존재 = idle.

    절차:
        1. 페르소나 ↔ tmux session 매핑 조회 (drafter 자율).
        2. 매핑 세션이 active_sessions 리스트에 없음 → status = idle, task = None.
        3. 있으면 capture-pane 마지막 N초 변화 비교 → working/idle.
        4. working이면 task 추론 (A-1 prompt > A-3 로그 > A-2 thinking, F-X-7-#5).
        5. tokens_k = TokenHook 인입 또는 capture-pane regex fallback.
    """
    pane_name = self._persona_to_pane(name)  # 예: "박지영" → "Orc-064-plan:1.1"
    if not pane_name or pane_name.split(":")[0] not in sessions:
        return PersonaState(
            name=name, team=team, status="idle", task=None,
            tokens_k=0.0, last_update=datetime.now(),
        )

    last_change = self.tmux.last_pane_change(pane_name)
    elapsed = (datetime.now() - last_change).total_seconds()
    if elapsed > 10.0:  # T 임계 10초 (Stage 8 settings.json 가능)
        return PersonaState(
            name=name, team=team, status="idle", task=None,
            tokens_k=self.token_hook.get_tokens_k(pane_name),
            last_update=last_change,
        )

    return PersonaState(
        name=name, team=team, status="working",
        task=self._infer_task(pane_name),  # A-1 > A-3 > A-2
        tokens_k=self.token_hook.get_tokens_k(pane_name),
        last_update=last_change,
    )
```

### 7.3 Q2 정확 hook 인터페이스 spec (drafter 자율 본문)

```python
# scripts/dashboard/token_hook.py

import json
from pathlib import Path
from typing import Optional
from .tmux_adapter import TmuxAdapter


class TokenHook:
    """Q2 정확 hook — claude CLI session-end hook 출력 인입 + capture-pane regex fallback."""

    HOOK_DIR = Path.home() / ".claude" / "dashboard_state"
    REGEX = r'"usage":\s*\{\s*"input_tokens":\s*(\d+),\s*"output_tokens":\s*(\d+)'

    def __init__(self, tmux: Optional[TmuxAdapter] = None) -> None:
        self.tmux = tmux or TmuxAdapter()

    def get_tokens_k(self, pane_name: str) -> float:
        """1순위 = hook 인입 / 2순위 = capture-pane regex / 3순위 = 0.0."""
        session = pane_name.split(":")[0]
        hook_value = self._read_hook(session)
        if hook_value is not None:
            return hook_value
        regex_value = self._regex_capture(pane_name)
        if regex_value is not None:
            return regex_value
        return 0.0

    def _read_hook(self, session: str) -> Optional[float]:
        f = self.HOOK_DIR / f"{session}.json"
        if not f.exists():
            return None
        try:
            data = json.loads(f.read_text())
            return (data["input_tokens"] + data["output_tokens"]) / 1000.0
        except (json.JSONDecodeError, KeyError, OSError):
            return None  # Sec.14 에러 경로 — fallback로 폴백

    def _regex_capture(self, pane_name: str) -> Optional[float]:
        ...  # capture-pane 마지막 100줄 → REGEX 매치 → tokens_k 변환
```

**`.claude/hooks/session-end.sh` 본문 (drafter 자율 의사 코드):**

```bash
#!/usr/bin/env bash
# Q2 정확 hook — claude CLI 세션 종료 시 토큰량 dump.
# 호출 컨텍스트: claude CLI hook (claude --teammate-mode tmux 통합).

SESSION_ID="${TMUX_SESSION:-${CLAUDE_SESSION_ID:-unknown}}"
OUT_DIR="$HOME/.claude/dashboard_state"
mkdir -p "$OUT_DIR"

# claude CLI 메타 stdin (또는 환경변수)에서 토큰량 추출
INPUT_TOKENS="${CLAUDE_INPUT_TOKENS:-0}"
OUTPUT_TOKENS="${CLAUDE_OUTPUT_TOKENS:-0}"

cat > "$OUT_DIR/${SESSION_ID}.json" <<EOF
{
  "input_tokens": $INPUT_TOKENS,
  "output_tokens": $OUTPUT_TOKENS,
  "ts": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
}
EOF
```

본 hook 인터페이스는 claude CLI hook 영역의 환경변수 / stdin spec에 의존하며, Stage 8 구현 시 실제 claude CLI hook API 검증이 필요한 영역으로 보입니다(Stage 8 이월 AC-S8-2). 환경변수 미존재 시 `0` 폴백 → `_regex_capture` 자동 폴백 체인이 작동합니다.

### 7.4 갱신 주기 + idle 임계값 확정 (Stage 5 이월 #2 처리)

| 변수 | 채택값 | 근거 (drafter 자율) |
|------|------|----------|
| **polling 주기** | **1초** | brainstorm 의제 3 1~2초 잠정 + Q2 정확 hook = 파일 read 비용 ≤ 5ms × 19명 = 100ms 이하. 1초 여유 충분. |
| **idle 임계 T** | **10초** | F-M2-S5c 후보 3/10/30초 중. 3초 = CPU 부하 / 30초 = 운영자가 대기 인식. 10초 = brainstorm 의제 3 "한눈에" 정합. |
| **stale 임계** | **2초** | last_update가 polling 주기 + 1초보다 오래되면 stale 표시 (drafter 자율 신규). |

본 채택값은 Stage 8에서 `.claude/settings.json`에 `dashboard_polling_sec` / `dashboard_idle_threshold_sec` 키로 옮길 영역(자율 영역, Stage 8 이월 AC-S8-3)으로 보입니다.

### 7.5 작업명 추론 우선순위 (F-X-7-#5 본문 흡수)

옵션 A (capture-pane) 채택 시 **A-1 > A-3 > A-2** 우선순위:

```python
def _infer_task(self, pane_name: str) -> Optional[str]:
    """A-1 prompt > A-3 로그 > A-2 thinking — F-X-7-#5 reviewer 권장 흡수."""
    last_lines = self.tmux.capture_pane(pane_name, lines=50)

    # A-1: prompt 행 ">" 이후 텍스트
    for line in reversed(last_lines):
        m = re.search(r"^\s*>\s*(.{5,80})", line)
        if m:
            return self._extract_task_from_prompt(m.group(1))

    # A-3: 로그 라인 — "S\d+" / "Stage \d+" / "v\d+\.\d+"
    for line in reversed(last_lines):
        m = re.search(r"(v\d+\.\d+\.\d+[^\s]*|Stage \d+|S\d+ [^\s]+)", line)
        if m:
            return m.group(1)

    # A-2: thinking — 직전 작업 추정 (drafter 자율 → reviewer 보강 권고 R-2)
    return None
```

---

## Sec. 8. M3 — 렌더링 layer technical detail

### 8.1 `TeamRenderer` 인터페이스 본문

```python
# scripts/dashboard/team_renderer.py

from typing import Dict, List
from textual.containers import Vertical
from textual.widgets import Static
from .models import PersonaState


class TeamRenderer(Static):
    """M3 — 박스 3개(기획/디자인/개발) + 행 렌더링 + 다중 버전 sub-row.

    F-D1 본문 결정: 입력 = Dict[str, List[PersonaState]] (key = 한국어 팀명).
    """

    EMPTY_BOX_LABEL = "(팀 대기 중)"  # F-X-7-#6 옵션 A 흡수
    SUB_ROW_PREFIX = "└"               # F-M3-4 흡수
    TOKEN_FORMAT = "{:.1f}k"           # F-M3-3 흡수

    def render(self, teams_data: Dict[str, List[PersonaState]]) -> str:
        """드래프터 자율 — Vertical(Static * N) 구조 반환."""
        rendered = []
        for team in ["기획", "디자인", "개발"]:
            personas = teams_data.get(team, [])
            rendered.append(self._render_box(team, personas))
        return "\n".join(rendered)

    def _render_box(self, team: str, personas: List[PersonaState]) -> str:
        if not personas:
            return f"╔══ {team}팀 ══╗\n║ {self.EMPTY_BOX_LABEL} ║\n╚════════╝"

        grouped = self._group_by_name(personas)  # {"박지영": [PersonaState, PersonaState]}
        lines = [f"╔══ {team}팀 ══╗"]
        for name, states in grouped.items():
            lines.extend(self._render_persona(name, states))
        lines.append("╚════════╝")
        return "\n".join(lines)

    def _render_persona(self, name: str, states: List[PersonaState]) -> List[str]:
        first = states[0]
        sym = "◉ working" if first.status == "working" else "○ idle"
        out = [f"║ {name} [{sym}]"]
        if first.task:
            out.append(f"║   {first.task}")
        out.append(f"║   tokens: {self.TOKEN_FORMAT.format(first.tokens_k)}")
        for sub in states[1:]:  # 다중 버전 — F-M3-4 sub-row prefix
            sub_task = sub.task or "(idle)"
            out.append(f"║ {self.SUB_ROW_PREFIX} {sub_task}")
            out.append(f"║   tokens: {self.TOKEN_FORMAT.format(sub.tokens_k)}")
        return out

    def _group_by_name(self, personas: List[PersonaState]) -> Dict[str, List[PersonaState]]:
        out: Dict[str, List[PersonaState]] = {}
        for p in personas:
            out.setdefault(p.name, []).append(p)
        return out
```

### 8.2 박스 너비 + layout (drafter 자율)

| 영역 | 결정 |
|------|------|
| **3등분 너비** | textual `Horizontal` 컨테이너 + 각 박스 `width: 1fr` (균등 분배). 터미널 120~150 col 기준 한 박스 ≈ 40~50 col. |
| **박스 border** | textual CSS-like `border: solid` 잠정. 색값 = 디자인팀 boundary slot #2. |
| **박스 높이** | 가변 (`height: auto`) — F-M5-4 / R6 박스 높이 45줄 우려 = M3 단계 scroll 영역(Stage 6 디자인팀 시안 영역). drafter v1 = scroll 채택. |
| **status bar** | 박스 3개 위 `Header` 1행 (Q1 / F-D3) — Sec.11.1 본문. |

### 8.3 다중 버전 ≥ 3개 처리 (Q-M3-2 잔존 → 본 stage 결정)

drafter 자율 본문: 동일 페르소나가 3개 이상 버전 작업 중이면 **첫 2개 표시 + "... 외 N개 버전" 1행**으로 truncate합니다.

```python
MAX_SUB_ROWS = 1  # 첫 entry + sub-row 1개 = 총 2개 버전 표시 (drafter 자율)

def _render_persona(self, name, states):
    ...
    truncated = states[: MAX_SUB_ROWS + 1]
    overflow = len(states) - len(truncated)
    if overflow > 0:
        out.append(f"║   ... 외 {overflow}개 버전")
```

본 결정은 brainstorm 의제 3 "6~9개 행 동시 가독성" 정합 영역으로 보입니다. reviewer 보강 권고 R-3(MAX_SUB_ROWS = 1 vs 2 vs 0 구분).

---

## Sec. 9. M4 — Pending + 알림 layer technical detail

### 9.1 `PendingDataCollector` 인터페이스 본문

```python
# scripts/dashboard/pending_collector.py

import re
from pathlib import Path
from typing import List
from datetime import datetime
from .models import PendingPush, PendingQuestion
from .tmux_adapter import TmuxAdapter


class PendingDataCollector:
    """M4 — Pending Push/Q sync 수집. F-D1 본문 SRP 분리 (M2와 독립)."""

    DISPATCH_DIR = Path("dispatch")
    Q_PATTERN = re.compile(r"\*\*Q[\d-]+\*\*[^|\n]*\|[^|\n]*\|.*?결정")
    PUSH_PATTERN = re.compile(r"⏳\s*(.+?)$", re.MULTILINE)

    def __init__(self, tmux: TmuxAdapter = None) -> None:
        self.tmux = tmux or TmuxAdapter()

    def get_pending_pushes(self) -> List[PendingPush]:
        out: List[PendingPush] = []
        # 1. git status 파싱 — unpushed commits / staged but not committed
        # 2. dispatch md "⏳" 패턴 (read-only, drafter 자율)
        ...
        return out

    def get_pending_questions(self) -> List[PendingQuestion]:
        out: List[PendingQuestion] = []
        for f in self.DISPATCH_DIR.glob("*.md"):
            content = f.read_text(errors="ignore")
            for m in self.Q_PATTERN.finditer(content):
                out.append(self._parse_q(m.group(0), source=str(f)))
        return out
```

### 9.2 `PendingPushBox` / `PendingQBox` 위젯 (drafter 자율)

```python
# scripts/dashboard/pending_widgets.py

from textual.widgets import Static
from .models import PendingPush, PendingQuestion


class PendingPushBox(Static):
    EMPTY_LABEL = "✓ 대기 항목 없음"

    def update_data(self, pushes: list[PendingPush]) -> None:
        if not pushes:
            self.update(f"┌── Pending Push/Commit ──┐\n│ {self.EMPTY_LABEL}\n└─────────────┘")
            return
        lines = ["┌── Pending Push/Commit ──┐"]
        for p in pushes[:5]:  # 5개 truncate (drafter 자율)
            lines.append(f"│ ⏳ {p.description}")
            lines.append(f"│    Initiator: {p.initiator}")
        lines.append("└─────────────┘")
        self.update("\n".join(lines))


class PendingQBox(Static):
    EMPTY_LABEL = "✓ 결정 대기 없음"

    def update_data(self, questions: list[PendingQuestion]) -> None:
        # 동일 패턴, priority 기준 정렬 (drafter 자율)
        ...
```

### 9.3 Q3 osascript 본문 spec + Windows fallback 비교

```python
# scripts/dashboard/notifier.py

import subprocess
import sys
from abc import ABC, abstractmethod
from functools import lru_cache
from .models import PendingQuestion


class Notifier(ABC):
    """알림 채널 abstraction — Q3 운영자 결정 = osascript 기본."""

    @abstractmethod
    def notify(self, q: PendingQuestion) -> bool:
        ...


class OSAScriptNotifier(Notifier):
    """macOS 기본 — Pushover / CCNotify 회피 (Q3 운영자 결정)."""

    @lru_cache(maxsize=128)
    def _dedupe_check(self, dedupe_key: str) -> bool:
        """5분 TTL — 동일 dedupe_key 중복 발화 방지 (F-X-7-#3)."""
        return True

    def notify(self, q: PendingQuestion) -> bool:
        if not self._dedupe_check(q.dedupe_key()):
            return False  # 중복 — skip
        msg = self._sanitize(q.description)  # Sec.16 보안 — AppleScript injection 회피
        title = "jOneFlow"
        cmd = [
            "osascript",
            "-e",
            f'display notification "{msg}" with title "{title}" sound name "Submarine"',
        ]
        try:
            result = subprocess.run(cmd, capture_output=True, timeout=5.0, check=False)
            return result.returncode == 0
        except (subprocess.TimeoutExpired, OSError):
            return False  # Sec.14 에러 경로

    @staticmethod
    def _sanitize(s: str) -> str:
        """AppleScript injection 회피 — `"` / `\\` / 줄바꿈 escape."""
        return s.replace("\\", "\\\\").replace('"', '\\"').replace("\n", " ")


class WindowsNotifier(Notifier):
    """Q4 P1 유지 — skeleton만. v0.6.5+ 본 가동."""

    def notify(self, q: PendingQuestion) -> bool:
        # plyer 또는 win10toast 채택 (drafter 자율 비교 = Sec.10.2)
        # v0.6.4 = stub (운영자 결정 = P1 유지)
        return False


def get_notifier() -> Notifier:
    if sys.platform == "darwin":
        return OSAScriptNotifier()
    elif sys.platform == "win32":
        return WindowsNotifier()
    else:
        # Linux / WSL — Q3 fallback skip (Stage 5 이월 처리 영역)
        return WindowsNotifier()  # noop stub
```

### 9.4 dedupe TTL 5분 알고리즘 (F-X-7-#3 본문)

`PendingQuestion.dedupe_key()`가 timestamp를 분 단위로 안정화하므로(Sec.3.2), `lru_cache(maxsize=128)`은 자연스럽게 5분 단위 dedupe를 구현합니다. 정확한 TTL 만료(시간 기반 expiry)가 필요하면 Stage 8에서 `cachetools.TTLCache(maxsize=128, ttl=300)` 교체 영역으로 보입니다(Stage 8 이월 AC-S8-4).

---

## Sec. 10. M5 — Windows + 18명 매핑 + status bar technical detail

### 10.1 Windows skeleton (Q4 P1 유지 본문)

```python
# scripts/dashboard/platform_compat.py

import sys
import platform
from typing import Literal

PlatformKind = Literal["macos", "windows", "linux", "wsl"]


def detect_platform() -> PlatformKind:
    """Q4 P1 유지 — macOS 단독 본 가동 + Windows skeleton.

    WSL 감지 = /proc/version 안 'microsoft' / 'WSL' 키워드.
    """
    if sys.platform == "darwin":
        return "macos"
    if sys.platform == "win32":
        return "windows"
    if sys.platform.startswith("linux"):
        try:
            with open("/proc/version") as f:
                if "microsoft" in f.read().lower() or "WSL" in f.read():
                    return "wsl"
        except OSError:
            pass
        return "linux"
    return "linux"  # default fallback


def supports_native_notification() -> bool:
    return detect_platform() == "macos"  # v0.6.4 = macOS 단독 본 가동
```

### 10.2 Windows 알림 채널 비교 (Stage 5 이월 #12 처리)

| 채널 | 권장도 | 장점 | 단점 |
|------|------|------|------|
| **plyer** | **★★★★ (0순위)** | cross-platform 통합 / pip 단일 / Linux 확장 가능 | 스타일 제한 (icon X) |
| **win10toast** | ★★★ (1순위) | Windows native toast / icon 지원 | Windows 한정 / Python 3.8+ 호환 검증 필요 |
| **winrt** | ★★ (2순위) | UWP API 최신 | Python binding 불안정 / 학습 비용 |
| **Pushover** | ❌ 회피 | cross-device / 모바일 | $5/mo 비용 / 외부 API (DEFCON) |

본 결정은 Q3 운영자 결정 = "Windows = plyer or win10toast Stage 5 검토" 본문 흡수입니다. drafter 권고 = **plyer 0순위**, 본 가동 검증은 v0.6.5+ 이월(Q4).

### 10.3 19명 표시 (Q1 / F-D3 final 본문)

> **운영자 결정 인수 (Q1 = F-D3 final, 세션 27 Stage 4.5 게이트 PASS):**
> v0.6.4 대시보드 표시 페르소나 범위 = **18명 박스 + PM 스티브 리 별도 상단 status bar 1행 = 총 19명 표시**. CTO 백현진 / CEO 이형진 미표시.

```python
# scripts/dashboard/status_bar.py

from textual.widgets import Static
from .models import PersonaState


class PMStatusBar(Static):
    """Q1 / F-D3 final — PM 스티브 리 (19번째) 상단 status bar 1행.

    layout: 가로 폭 전체. format = "PM 스티브 리 [상태] | <task> | tokens: <N>k | active orcs: <list>"
    """

    PERSONA_NAME = "스티브 리"

    def update_data(self, pm_state: PersonaState, active_orcs: list[str]) -> None:
        sym = "◉" if pm_state.status == "working" else "○"
        orcs = " ".join(active_orcs) if active_orcs else "(idle)"
        line = (
            f"PM {pm_state.name} [{sym} {pm_state.status}] | "
            f"{pm_state.task or '(idle)'} | "
            f"tokens: {pm_state.tokens_k:.1f}k | "
            f"active orcs: {orcs}"
        )
        self.update(line)
```

PM 스티브 리는 `PersonaDataCollector.PERSONAS_18` 리스트와 별도로 19번째 페르소나로 처리됩니다(Sec.7.1). drafter 자율 본문 = `PERSONAS_18`은 박스 영역만, PM은 `_fetch_pm_state()` 별도 메서드로 `tmux session bridge-064` 인입 (Stage 8 구현 영역).

### 10.4 18명 매핑 detail boundary

planning_index Sec.7 이월 #11 = "18명 매핑 detail (`personas_18.md` 도착 후)" — F-X-6 boundary에 따라 **Stage 8 진입 = blocking 시점**입니다. 본 stage는 `PERSONAS_18` 리스트(이름 + 팀)만 박고, 페르소나별 데이터 수집 detail(예: 박지영=`Orc-064-plan:1.1` capture-pane / 카더가든=`Orc-064-dev:1.2` 등)은 Stage 8 진입 직전에 매핑 (M5 / Orc-064-design 영역 sync).

---

## Sec. 11. 운영자 결정 5/5 본문 흡수 (Q1~Q5)

본 섹션은 dispatch Sec.핵심작업 D 흡수 본문입니다. 각 결정의 인터페이스 spec 위치를 명시합니다.

### 11.1 Q1 = F-D3 final (19명 표시) — Sec.10.3 박힘

- **결정:** 18명 박스 + PM 스티브 리 별도 상단 status bar 1행 = 19명 표시 / CTO·CEO 미표시.
- **인터페이스 spec 위치:** Sec.10.3 `PMStatusBar` + Sec.6.1 `compose()` 구조.
- **M3 layout 영향:** 박스 3개 위 `Header` 1행 추가. Stage 6/7 디자인팀 시안 영역 (디자인팀 boundary slot #5 신규).

### 11.2 Q2 = 정확 hook (+8h) — Sec.7.3 박힘

- **결정:** `~/.claude/hooks/session-end.sh` + `.claude/dashboard_state/<session_id>.json` 파일 인입. 폴백 = capture-pane regex.
- **인터페이스 spec 위치:** Sec.7.3 `TokenHook` 클래스 + `.claude/hooks/session-end.sh` 의사 코드.
- **Stage 8 +8h 인정:** hook 인프라 신규 (claude CLI hook API 검증 + 환경변수 매핑 + 테스트 fixture). 본 stage 결정으로 Stage 8 작업량 명시 → planning Score 가중치 반영(Sec.18 finalizer 영역).

### 11.3 Q3 = osascript / Pushover 회피 — Sec.9.3 박힘

- **결정:** macOS = osascript subprocess.run. Windows fallback = plyer 0순위. CCNotify 회피 (운영자 결정 = 신설 안 함). Pushover 회피 (외부 API + 비용 = DEFCON).
- **인터페이스 spec 위치:** Sec.9.3 `OSAScriptNotifier` + Sec.10.2 Windows 비교표.
- **dedupe:** 5분 TTL `lru_cache(maxsize=128)` (F-X-7-#3 흡수).
- **보안:** AppleScript injection 회피 `_sanitize()` (Sec.16.2).

### 11.4 Q4 = P1 유지 (Windows skeleton) — Sec.10.1 박힘

- **결정:** v0.6.4 = macOS 단독 본 가동 + Windows skeleton만 (plyer / win10toast 인터페이스 stub). 본 가동 검증 = v0.6.5+ 이월.
- **인터페이스 spec 위치:** Sec.10.1 `platform_compat.py` + Sec.9.3 `WindowsNotifier` stub.
- **M5 AC 분리:** AC-M5-3a (skeleton presence) 본 stage 자동 검증 / AC-M5-3b (18명 detail) Stage 8 이월.

### 11.5 Q5 = idle 통합 confirm — Sec.7.2 박힘

- **결정:** tmux 세션 미존재 = `status="idle"` 통합. offline 분리 옵션 0건. brainstorm 의제 3 2단계 정책 유지.
- **인터페이스 spec 위치:** Sec.7.2 `_infer_state` + Sec.3.1 `PersonaStatus = Literal["working", "idle"]`.
- **M3 렌더링 정합:** idle 페르소나 task = None → 행 2(task) 생략, 행 3(tokens_k 0.0)만 표시.

### 11.6 Q1~Q5 흡수 검증 표 (자동)

| Q | 본문 박힘 위치 | 자동 검증 |
|---|------|---------|
| Q1 (F-D3) | Sec.10.3 `PMStatusBar` | `grep -cE "PMStatusBar\|status bar 1행\|19명" docs/03_design/v0.6.4_technical_design.md` ≥ 3 |
| Q2 정확 hook | Sec.7.3 `TokenHook` + `.claude/hooks/session-end.sh` | `grep -cE "TokenHook\|session-end.sh\|dashboard_state" docs/03_design/v0.6.4_technical_design.md` ≥ 3 |
| Q3 osascript | Sec.9.3 `OSAScriptNotifier` | `grep -cE "OSAScriptNotifier\|display notification\|Pushover.*회피" docs/03_design/v0.6.4_technical_design.md` ≥ 3 |
| Q4 P1 유지 | Sec.10.1 `platform_compat.py` | `grep -cE "platform_compat\|P1 유지\|Windows skeleton" docs/03_design/v0.6.4_technical_design.md` ≥ 3 |
| Q5 idle 통합 | Sec.7.2 `_infer_state` | `grep -cE "idle 통합\|tmux 미존재\|offline 분리.*0건" docs/03_design/v0.6.4_technical_design.md` ≥ 3 |

---

## Sec. 12. Stage 5 이월 14건 처리 (planning_index Sec.7 단일 source)

| # | 항목 | 본 stage 처리 (drafter v1) |
|---|------|---------|
| **1** | 데이터 수집 방식 (capture-pane / hook / 자가보고) | ✅ 본문 결정 — 옵션 A capture-pane + Q2 hook 인입 폴백 (Sec.7.2). 자가보고 옵션 회수. |
| **2** | 갱신 주기 + idle 임계 T | ✅ 본문 결정 — polling 1초 / idle T 10초 / stale 2초 (Sec.7.4). |
| **3** | textual CSS 색상 팔레트 | 🟡 디자인팀 boundary slot #1 (Sec.13). |
| **4** | 진행률 바 / 스파크라인 | 🟡 디자인팀 boundary slot #3, #4 (Sec.13). |
| **5** | 토큰량 정확 vs 추정 | ✅ 본문 결정 — Q2 정확 hook 채택 (Sec.7.3). |
| **6** | sync vs async 인터페이스 | ✅ 본문 결정 — Threaded sync wrapper 채택 (Sec.5). |
| **7** | 작업명 추론 우선순위 | ✅ 본문 결정 — A-1 > A-3 > A-2 (Sec.7.5). |
| **8** | Pending 캐치 hook | ✅ 본문 결정 — `Q_PATTERN` regex + dispatch md glob (Sec.9.1). capture-pane 폴백은 자가보고 옵션 회수와 정합. |
| **9** | 알림 채널 채택 | ✅ 본문 결정 — Q3 osascript 기본 / plyer Windows / Pushover·CCNotify 회피 (Sec.9.3). |
| **10** | 알림 dedupe 알고리즘 | ✅ 본문 결정 — 5분 TTL `lru_cache(maxsize=128)` + `dedupe_key()` 분 단위 안정화 (Sec.9.4 / 3.2). |
| **11** | 18명 매핑 detail | 🟡 Stage 8 진입 직전 = `personas_18.md` blocking (F-X-6, Sec.10.4). 본 stage = `PERSONAS_18` 리스트만. |
| **12** | Windows 알림 채널 비교 | ✅ 본문 결정 — plyer 0순위 / win10toast 1순위 비교표 (Sec.10.2). 본 가동 = Q4 P1 유지로 v0.6.5+ 이월. |
| **13** | 6~9행 가독성 visual 검증 | 🟡 디자인팀 boundary slot — Stage 6 협업 영역 (Sec.13). |
| **14** | 디자인팀 첫 등판 dispatch | 🟡 디자인팀 boundary slot — Stage 5 완료 후 회의창 + 박지영 PL 자율 영역 (Sec.13.5). |

**처리 결과:** 본문 결정 9건 (1, 2, 5, 6, 7, 8, 9, 10, 12) + 디자인팀 boundary 5건 (3, 4, 13, 14) + Stage 8 이월 1건 (11) = **9 + 5 = 14**. dispatch Sec.핵심작업 E "본문 결정 9건 + boundary 5건" 정합.

---

## Sec. 13. 디자인팀 첫 등판 boundary (Stage 6/7 인터페이스)

본 stage는 **boundary slot 4건**만 박고, 본문(색값 / 픽셀 / 시안)은 디자인팀(Orc-064-design) 영역으로 위임합니다.

### 13.1 색상 토큰 정의 영역 (slot #1)

```css
/* scripts/dashboard/dashboard.tcss (Stage 6 디자인팀 영역) */

/* drafter v1 = 토큰명 슬롯만, 색값은 Stage 6 영역 */
$color-working: <Stage 6>;     /* 활성 페르소나 강조 */
$color-idle: <Stage 6>;         /* 대기 페르소나 dim */
$color-pending-high: <Stage 6>; /* Pending Q priority=high */
$color-pending-low: <Stage 6>;
$color-stale: <Stage 6>;        /* last_update > 2초 — drafter 신규 토큰 */
$color-pm-bar: <Stage 6>;       /* Q1 status bar 1행 강조 */
```

### 13.2 박스 border / margin / padding (slot #2)

textual `Static` border 시안 (drafter v1 기본 `border: solid` / margin·padding = Stage 6).

### 13.3 진행률 바 / 스파크라인 (slot #3, #4)

drafter v1 미구현 — Stage 6/7 디자인팀 시안 후 Stage 8 추가 영역 (M3 F-M3-S5-4 sync).

### 13.4 6~9행 visual 검증 시나리오 (slot #5 = 신규, R6 / F-M5-4 흡수)

박스 높이 45줄(개발팀 7명 × 3행 + 마진) 우려 — Stage 6 디자인팀이 (a) scroll vs (b) collapse 결정. drafter 권고 = scroll 채택, collapse는 Stage 6 시안 후 Stage 8 가능 영역.

### 13.5 디자인팀 첫 등판 dispatch boundary

본 dispatch 본문 작성은 회의창(CTO 실장) + 박지영 PL 자율 영역입니다 (planning_index Sec.9 / F-X-10). 본 stage는 Stage 6 진입 시점에 디자인팀이 받을 입력 인터페이스만 박습니다:

| 디자인팀 입력 | 본 stage 산출 위치 |
|----------|-----------|
| M3 plan_final | `docs/02_planning_v0.6.4/planning_03_M3_render.md` |
| M3 layout 골격 | 본 문서 Sec.8 |
| 색상 토큰 슬롯 | 본 문서 Sec.13.1 |
| 18명 매핑 슬롯 | `docs/02_planning_v0.6.4/planning_05_M5_windows_personas.md` Sec.B |
| status bar 1행 layout | 본 문서 Sec.10.3 / Sec.11.1 |

---

## Sec. 14. 에러 경로 (drafter 자율 본문)

### 14.1 분류

| 분류 | 발생 영역 | 처리 정책 |
|------|---------|---------|
| **A. tmux unavailable** | TmuxAdapter — `subprocess.run("tmux ls")` FAIL | 모든 페르소나 status="idle" 폴백 + status bar에 "⚠ tmux 미감지" 1행 표시 |
| **B. capture-pane 파싱 실패** | TmuxAdapter regex mismatch | 해당 페르소나만 task=None 폴백 + last_update 갱신 X (stale 표시 자연 발생) |
| **C. token hook 파일 손상** | TokenHook JSON parse fail | tokens_k=0.0 폴백 + 다음 polling에 hook 파일 재read 시도 |
| **D. dispatch md 미존재** | PendingDataCollector glob 0건 | PendingPushBox / PendingQBox = "✓ 대기 항목 없음" 표시 |
| **E. osascript 실패** | Notifier subprocess timeout / returncode != 0 | `notify()` False 반환, 다음 Q 도착 시 재시도 |
| **F. Worker thread 예외** | refresh_loop unhandled exception | `call_from_thread(_show_stale)` 호출 + 1초 sleep 후 다음 iteration |

### 14.2 폴백 체인 (drafter 자율)

```
Q2 토큰량:
  hook 파일 read → JSON parse → 성공 → 반환
                              → 실패 → capture-pane regex 폴백
                                       → 성공 → 반환
                                       → 실패 → 0.0 반환

상태 추론:
  tmux session 존재? → No → idle 폴백 (Q5)
                     → Yes → last_pane_change → idle 임계 T 비교
                                              → working 또는 idle
```

### 14.3 staleness 표시 영역 (drafter 자율 신규)

`PersonaState.last_update`가 polling 주기 + 1초(= 2초)보다 오래 stale이면 status 기호 옆에 `⚠`를 표시합니다:

```
║ 박지영 [◉ working ⚠]
║   v0.6.4/Stage 5
║   tokens: 145.2k (stale)
```

본 정책은 운영자가 "데이터 신선도"를 즉각 인지할 수 있도록 합니다. drafter 자율 → reviewer 보강 권고 R-4 (운영자 가시성 vs UI 노이즈 trade-off).

---

## Sec. 15. 테스트 전략 (drafter 자율 본문)

### 15.1 단위 테스트 분담

| 모듈 | 테스트 파일 | 핵심 케이스 |
|------|----------|------|
| `models.py` | `test_models.py` | dataclass 필드 / `to_json()` round-trip / `dedupe_key()` 분 단위 안정화 |
| `persona_collector.py` | `test_persona_collector.py` | tmux mock fixture × 18명 + Q5 idle 통합 + 작업명 우선순위 A-1>A-3>A-2 |
| `pending_collector.py` | `test_pending_collector.py` | dispatch md fixture + Q_PATTERN regex + git status mock |
| `team_renderer.py` | `test_team_renderer.py` | 빈 박스 / 단일 / 다중 버전 sub-row / ≥3개 truncate / token format `%.1fk` |
| `notifier.py` | `test_notifier.py` | osascript subprocess mock + dedupe 5분 TTL + sanitize escape |
| 횡단 dedupe | `test_dedupe.py` | 동일 dedupe_key 5분 내 2번 호출 → 첫 번째만 발화 |

### 15.2 테스트 fixture 영역 (drafter 자율)

```python
# scripts/dashboard/tests/fixtures.py

SAMPLE_TMUX_LS = """\
bridge-064: 1 windows (created Sun Apr 27 10:00:00 2026)
Orc-064-dev: 4 windows (created Sun Apr 27 14:00:00 2026)
"""

SAMPLE_CAPTURE_PANE_WORKING = """\
> v0.6.4 Stage 5 technical_design v1 작성 중
[Running...]
"usage": {"input_tokens": 145000, "output_tokens": 35000}
"""

SAMPLE_DISPATCH_MD_Q = """\
| **Q1** | 박스 외 페르소나 표시 정책 | M5 / 횡단 | 운영자 결정 |
"""
```

### 15.3 covering 자동 검증 (drafter 자율 → finalizer 영역)

| 영역 | 자동 검증 명령 |
|------|----------|
| 단위 테스트 통과 | `cd scripts/dashboard && python3 -m pytest tests/ -v` exit 0 |
| 모듈 경계 4원칙 | Sec.4.3 grep 표 4건 |
| read-only AC | `grep -cE "git push\|git commit\|open\(.*['\"]w['\"]" scripts/dashboard/*.py` = 0 |
| import 순환 | `python3 -c "import scripts.dashboard"` exit 0 |

### 15.4 Stage 9 코드 리뷰 자동 검증 영역 (Stage 9 이월 sync)

- `call_from_thread` 사용 위치 검증 — Worker thread에서 위젯 직접 변경 시 FAIL
- `subprocess.run` timeout 누락 검증 — Sec.16.1 보안 정책 위반 시 FAIL
- AppleScript injection sanitize 검증 — Sec.16.2 위반 시 FAIL

---

## Sec. 16. 보안 고려사항 (drafter 자율 본문)

### 16.1 subprocess timeout 영구 정책

본 v0.6.4 모든 `subprocess.run()` 호출에 `timeout=` 인자 필수:

| 호출 위치 | timeout |
|---------|---------|
| `tmux_adapter.py` capture-pane | 2.0초 |
| `tmux_adapter.py` list-sessions | 2.0초 |
| `notifier.py` osascript | 5.0초 |
| `token_hook.py` (subprocess 사용 시) | 2.0초 |

자동 검증 = `grep -cE "subprocess.run.*timeout=" scripts/dashboard/*.py` ≥ 3 / `grep -c "subprocess.run" scripts/dashboard/*.py` 비교 (모든 호출에 timeout 필수, AC-T-5).

### 16.2 AppleScript injection 회피 (Q3 정합)

`OSAScriptNotifier._sanitize()` (Sec.9.3)가 PendingQuestion.description에 포함된 `"` / `\\` / 줄바꿈을 escape합니다. 사용자 입력 / dispatch md 내용이 AppleScript 명령으로 직접 삽입되지 않도록 영구 정책으로 박습니다.

```python
# 위반 사례 (drafter 자율 — reviewer 보강 R-5)
description = '"; do shell script "rm -rf /tmp/*"; --'
# sanitize 미적용 시 osascript에 그대로 전달 → 임의 명령 실행 위험
# sanitize 적용 시 → '\"; do shell script ... --' (escape 처리)
```

### 16.3 read-only 영구 검증 (F-X-2 정합)

5개 plan_final read-only 정책 영구 적용 — 본 stage AC-S-1로 자동 검증:

```bash
grep -cE "git push|git commit|open\(.*['\"]w['\"]|os\.remove|shutil\.rmtree" scripts/dashboard/*.py 2>/dev/null \
  | awk -F: '{s+=$2} END{print s+0}'
# 기대값: 0
```

### 16.4 secret_loader 사용 영역 (DEFCON 회피)

본 v0.6.4는 외부 API 호출 0건 (Q3 Pushover 회피)이라 `secret_loader.py` 사용 영역 없음. Pushover 채택 분기에서만 `load_secret("PUSHOVER_TOKEN")` 영역이 발생할 수 있어 v0.6.4 scope에서는 영구 회수.

### 16.5 git pre-commit hook 권장 (R5 / F-M4-5 흡수)

Stage 8에서 `pre-commit` hook 추가 권고:

```bash
# .git/hooks/pre-commit 또는 pre-commit framework
grep -rE "git push|git commit|open\(.*['\"]w" scripts/dashboard/ && exit 1
```

본 hook은 Stage 8 구현 영역(자율 영역, AC-S8-5)으로 보이며, 본 stage는 권고만 명시합니다.

---

## Sec. 17. AC 명세 (자동 / 수동 / Stage 8 이월 3분리)

> **drafter v1 통계:** 자동 22 + 수동 4 + Stage 8 이월 5 = **31건**. 자동 비율 = 22 / (22+4) = **84.6%** (planning 73% 상향).

### 17.1 자동 검증 AC (22건)

| AC ID | 기준 | 측정 명령 (1줄) |
|------|------|----------|
| **AC-D-1** | F-D1 dataclass 3종 정의 | `grep -cE "@dataclass" scripts/dashboard/models.py` = 3 |
| **AC-D-2** | F-D2 진입점 단일 | `test -f scripts/dashboard.py && test -d scripts/dashboard/` |
| **AC-D-3** | F-D2 모듈 11개 presence | `ls scripts/dashboard/*.py \| wc -l` ≥ 11 |
| **AC-D-4** | F-D4 sync 인터페이스 | `grep -cE "async def" scripts/dashboard/persona_collector.py scripts/dashboard/pending_collector.py` = 0 |
| **AC-D-5** | F-D4 thread worker | `grep -cE "run_worker.*thread=True" scripts/dashboard.py` ≥ 1 |
| **AC-T-1** | 모듈 경계 #1 단방향 | `grep -lE "from .*team_renderer\|from .*pending_widgets" scripts/dashboard/{persona,pending,tmux}_*.py 2>/dev/null \| wc -l` = 0 |
| **AC-T-2** | 모듈 경계 #2 read-only | `grep -cE "git push\|git commit\|open\(.*['\"]w['\"]" scripts/dashboard/*.py 2>/dev/null \| awk -F: '{s+=$2} END{print s+0}'` = 0 |
| **AC-T-3** | 모듈 경계 #3 dataclass 단일 | `grep -cE "@dataclass" scripts/dashboard/*.py \| awk -F: '{s+=$2} END{print s+0}'` = 3 |
| **AC-T-4** | 모듈 경계 #4 subprocess 격리 | `grep -lE "import subprocess" scripts/dashboard/*.py \| wc -l` = 3 |
| **AC-T-5** | subprocess timeout 영구 | `python3 -c "import re,glob; [exit(1) for f in glob.glob('scripts/dashboard/*.py') if re.search(r'subprocess\.run\(', open(f).read()) and not re.search(r'subprocess\.run\([^)]*timeout=', open(f).read())]"` exit 0 |
| **AC-Q1-1** | Q1 status bar 1행 | `grep -cE "PMStatusBar\|status bar 1행\|19명" docs/03_design/v0.6.4_technical_design.md` ≥ 3 |
| **AC-Q1-2** | Q1 status_bar.py presence | `test -f scripts/dashboard/status_bar.py` |
| **AC-Q2-1** | Q2 token_hook.py | `test -f scripts/dashboard/token_hook.py && grep -c "TokenHook" scripts/dashboard/token_hook.py` ≥ 1 |
| **AC-Q2-2** | Q2 hook 디렉토리 spec | `grep -cE "dashboard_state\|session-end\.sh" docs/03_design/v0.6.4_technical_design.md` ≥ 3 |
| **AC-Q3-1** | Q3 osascript 본문 | `grep -cE "osascript\|display notification\|OSAScriptNotifier" scripts/dashboard/notifier.py` ≥ 3 |
| **AC-Q3-2** | Q3 Pushover/CCNotify 회피 | `grep -ciE "Pushover\|CCNotify" scripts/dashboard/notifier.py` = 0 |
| **AC-Q3-3** | Q3 dedupe TTL 5분 | `grep -cE "lru_cache\(maxsize=128\)\|TTL=300\|5분" docs/03_design/v0.6.4_technical_design.md scripts/dashboard/notifier.py` ≥ 2 |
| **AC-Q4-1** | Q4 platform_compat | `test -f scripts/dashboard/platform_compat.py && grep -c "detect_platform" scripts/dashboard/platform_compat.py` ≥ 1 |
| **AC-Q5-1** | Q5 idle 통합 | `grep -cE "idle 통합\|tmux 미존재.*idle\|offline.*0건" docs/03_design/v0.6.4_technical_design.md` ≥ 3 |
| **AC-S-1** | read-only 영구 (F-X-2) | `grep -cE "git push\|git commit\|open\(.*['\"]w['\"]" scripts/dashboard/*.py 2>/dev/null \| awk -F: '{s+=$2} END{print s+0}'` = 0 |
| **AC-S-2** | sanitize injection 회피 | `grep -c "_sanitize\|escape" scripts/dashboard/notifier.py` ≥ 1 |
| **AC-DOC-1** | F-D 본문 결정 3건 박힘 | `grep -cE "F-D1 본문 결정\|F-D2 본문 결정\|F-D4 본문 결정" docs/03_design/v0.6.4_technical_design.md` ≥ 3 |

### 17.2 수동 검증 AC (4건)

| AC ID | 기준 | 측정 |
|------|------|------|
| **AC-V-1** | 박스 너비 33% × 3 | textual dev 화면 (터미널 120~150 col) visual 검사 |
| **AC-V-2** | 다중 버전 sub-row visual | 동일 페르소나 2건 PersonaState 시뮬레이션 → `└` prefix 가시 확인 |
| **AC-V-3** | osascript 알림 발화 | macOS 환경 → `osascript` 직접 실행 → 알림센터 가시 확인 |
| **AC-V-4** | 6~9행 가독성 (R6 / F-M5-4) | 18명 모두 active fixture → 박스 높이 45줄 scroll 동작 확인 (Stage 6 디자인팀 영역과 sync) |

### 17.3 Stage 8 이월 AC (5건, 자동/수동 분모 외)

| AC ID | 기준 | 시점 |
|------|------|------|
| **AC-S8-1** | 18명 personas_18.md detail 매핑 | Stage 8 진입 직전 (F-X-6 blocking) |
| **AC-S8-2** | claude CLI hook API 검증 | Stage 8 (Q2 hook 환경변수 / stdin spec 실험) |
| **AC-S8-3** | `.claude/settings.json` 키 박음 | Stage 8 (`dashboard_polling_sec` / `dashboard_idle_threshold_sec`) |
| **AC-S8-4** | `cachetools.TTLCache` 정확 dedupe | Stage 8 (Q3 dedupe 시간 기반 expiry) |
| **AC-S8-5** | git pre-commit hook 추가 | Stage 8 (R5 / F-M4-5) |

### 17.4 AC 자동성 비율 표

| 분류 | 건수 | 비율 |
|------|------|------|
| 자동 | 22 | 84.6% (22 / 26) |
| 수동 | 4 | 15.4% (4 / 26) |
| **자동+수동 합** | **26** | **100% (분모)** |
| Stage 8 이월 (분모 외) | 5 | — |
| **총합** | **31** | — |

**자동 비율 = 84.6%** — planning_index 73% 대비 +11.6%p 상향. dispatch Sec.G 목표(자동 ≥ 70%) 통과로 보입니다.

---

## Sec. 18. Score (drafter 추정 → finalizer 확정 영역)

| 기준 | 가중 | drafter 추정 점수 | 근거 |
|------|------|----------|------|
| F-D 본문 결정 (3건 D1/D2/D4) | 20 | 19/20 | 3건 본문 박힘. D3 final은 Q1 흡수로 본 stage 영역 외(Stage 4.5 이미 PASS). |
| 운영자 결정 5/5 흡수 (Q1~Q5) | 15 | 15/15 | 인터페이스 spec 위치 명시 (Sec.11.6 자동 검증 표) |
| AC 자동 비율 (목표 ≥ 70%) | 15 | 15/15 (84.6%) | +11.6%p 상향 |
| Stage 5 이월 14건 처리 | 10 | 9/10 | 본문 결정 9 + boundary 5 = 14 정합 |
| 모듈 경계 / 데이터 흐름 | 10 | 9/10 | 4원칙 + 자동 검증 grep 표 |
| 에러 경로 / 테스트 / 보안 | 10 | 9/10 | A~F 분류 + 단위 테스트 분담 + injection 회피 |
| 디자인팀 boundary slot | 5 | 5/5 | slot #1~#5 명시 |
| Q1~Q5 본문 흡수 자동 검증 표 | 5 | 5/5 | Sec.11.6 |
| 헌법 위반 0건 | 5 | 5/5 | drafter 영역만, Agent tool 분담 X / pane 이탈 X / 직접 push X |
| **drafter v1 추정 Score** | 100 | **91/100** | reviewer 보강 + finalizer 확정 영역 (R-1~R-5 처리 후 95~98% 가능 영역) |

본 Score는 drafter v1 추정값입니다. reviewer(최우영) 보강 권고 R-1~R-5 처리 + finalizer(현봉식) 통합 verdict 후 design_final.md에서 확정 박힘 영역으로 보입니다. 임계 80% 초과 — Stage 6/7 진입 게이트 통과 후보.

---

## Sec. 19. 변경 이력

| 날짜 | 개정 | 비고 |
|------|------|------|
| 2026-04-27 | **v1 초안 (카더가든 drafter, 개발팀 백앤드 주임연구원, Haiku/medium, 세션 28)** | Stage 5 dispatch 본문 영역(A~G) + 자율 영역(M1~M5 모듈 경계 / 데이터 흐름 / 에러 경로 / 테스트 / 보안) 박음. **F-D 본문 결정 3건** (F-D1 PersonaState 6필드 + dataclass 3종 / F-D2 `scripts/dashboard.py` 단일 진입 + 11개 모듈 패키지 / F-D4 Threaded sync wrapper 채택). **운영자 결정 5/5 흡수** (Q1 status bar 1행 + Q2 정확 hook + Q3 osascript / Pushover 회피 + Q4 P1 유지 + Q5 idle 통합 confirm). **Stage 5 이월 14건 처리**: 본문 결정 9건(1, 2, 5, 6, 7, 8, 9, 10, 12) + 디자인팀 boundary 5건(3, 4, 13, 14) + Stage 8 이월 1건(11). **AC 31건** (자동 22 / 수동 4 / Stage 8 이월 5, 자동 비율 84.6%). 디자인팀 boundary slot 5건 명시. status: drafter v1 → reviewer (최우영) 보강 대기. R-1~R-5 reviewer 영역 표시. |

---

## 첨부 A: drafter 자율 영역 적용 trail

| 자율 영역 | drafter v1 채택 | reviewer 보강 권고 마커 |
|---------|------|-----------|
| F-D1 dataclass 필드 / 직렬화 | 6/6/6 필드 + `to_json()` `dataclasses.asdict()` 기준 + `dedupe_key()` 분 단위 안정화 | R-1 (stale 임계 2초 vs 갱신주기 동일) |
| F-D2 디렉토리 detail | `scripts/dashboard/` 11개 파일 + `tests/` 6건 + `.claude/hooks/` + `dashboard_state/` | — |
| F-D4 sync/async 채택 근거 | Threaded sync wrapper (옵션 C) — 옵션 A/B trade-off 표 | — |
| Q2 정확 hook 인터페이스 spec | `~/.claude/hooks/session-end.sh` + JSON 파일 인입 + capture-pane regex 폴백 체인 | — |
| Q3 osascript 명령 형식 | `display notification "<msg>" with title "<title>" sound name "Submarine"` + `_sanitize()` injection 회피 | R-5 (sound name 영역 디자인팀 vs drafter) |
| Q3 Windows fallback 비교 | plyer 0순위 / win10toast 1순위 / winrt 2순위 / Pushover·CCNotify 회피 | — |
| AC 표 형식 / 자동화 명령 1줄 | 자동 22 + 수동 4 + Stage 8 이월 5 (3분리) | — |
| 작업명 추론 우선순위 | A-1 prompt > A-3 로그 > A-2 thinking | R-2 (A-2 thinking 처리 = 직전 작업 추정) |
| MAX_SUB_ROWS 다중 버전 | `1` (첫 entry + sub-row 1개 = 2개 표시 + 외 N개 truncate) | R-3 (1 vs 2 vs 0) |
| staleness ⚠ 표시 | `last_update > 2초` → `⚠` 기호 추가 | R-4 (운영자 가시성 vs UI 노이즈) |
| AppleScript injection sanitize | `\\` / `"` / 줄바꿈 escape | R-5 |

---

## 첨부 B: 헌법 자가 점검 (drafter 본 작업 종료 직전)

| # | 점검 항목 | 결과 |
|---|---------|------|
| 1 | drafter pane (Orc-064-dev:1.2)에서 본문 작성했나? | ✅ 본 pane = :1.2 카더가든-be-drafter |
| 2 | Agent tool 분담 시도 0건? | ✅ Agent tool 사용 0건 |
| 3 | 직접 git push / 외부 API / `rm -rf` 0건? | ✅ Read/Write/Edit 도구만 사용, push 0건 |
| 4 | 페르소나 본분 위반(reviewer/finalizer 영역 침범) 0건? | ✅ R-N 마커 / Score 추정 / verdict X — 모두 reviewer/finalizer 영역으로 표시만 박음 |
| 5 | dispatch Sec.핵심작업 A~G 흡수? | ✅ A(Sec.3) / B(Sec.2) / C(Sec.5) / D(Sec.11) / E(Sec.12) / F(Sec.13) / G(Sec.17) |
| 6 | 자율 영역 본문 박음? | ✅ 첨부 A 11건 |
| 7 | DEFCON / 사고 12·13 재발 0건? | ✅ 회의창 강제 진입 / Agent 분담 / pane 이탈 / 답변≠행동 0건 |
| 8 | 중단 조건 발동 사유 0건? | ✅ Score 91% (≥80%) / AC 자동 84.6% (≥70%) / F-D 본문 3/3 / Q 5/5 / 헌법 위반 0건 |

---

**v1 초안 완료. 카더가든 (개발팀 백앤드 주임연구원, Haiku/medium). 본 산출물은 :1.3 최우영(be-reviewer) 보강 권고 입력으로 전달 영역으로 보입니다. drafter 영역 책임 종료, 본 pane(:1.2) 시그니처 1줄 출력 후 reviewer 대기 모드 전환합니다.**
