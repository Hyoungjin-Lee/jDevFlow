# dispatch: v0.6.6 인프라 정밀화 (Standard)

**발행:** 2026-04-27 | **대상:** bridge-066 → Orc-066-dev  
**모드:** Standard (Codex 12·14 생략, Claude 단독 리뷰)

---

## 결정 사항

Stage 01+02 다세션 논의로 확정. 기획 docs 생략, 본 dispatch가 기획 대체.

---

## 의제 5건 + 병렬 구도

**Round A (병렬):** #1 + #2 + #6  
**Round B (#4 완료 후):** #4 → #5

### #1 R3 디렉터리 실제 변경 ✅ 사전 승인
- `handoffs/active/` + `handoffs/archive/` 트리 정합
- `dispatch/archive/` 디렉토리 신규
- 기존 completed 파일들 git mv (archive 이동)
- 상세 권고 = `docs/r3_active_archive_plan.md` 참조

### #2 hook_stop.sh → completion_signal.sh 실제 연결
- `scripts/hook_stop.sh` 내부에 `scripts/completion_signal.sh` 호출 추가
- `.claude/settings.json` Stop hook 경로 확인
- `CLAUDE_COMPLETION_STAGE` 환경변수 전달 검증

### #6 A 패턴 drafter/reviewer 분리 강제 메커니즘
- dispatch 템플릿에 drafter/reviewer 별도 commit 강제 명시
- `scripts/` 또는 `docs/guides/` 에 A 패턴 체크리스트 추가
- R2 변형(단일 commit) 재발 방지

### #4 settings.json schema v0.5
- `stage_assignments` 키 13-stage → 16-stage 정합
  - `stage8_impl` → `stage11_impl`
  - `stage9_review` → `stage12_review`
  - `stage10_fix` → `stage13_fix`
  - `stage11_verify` → `stage14_verify`
- 기존 키 하위호환 주석 처리 (breaking change 최소화)

### #5 spawn_team.sh split panes 로직 보강
- #4 schema 변경 반영
- pane 4개 보장 로직 (오케 1 + 팀원 3)
- `@persona` set-option 자동 적용
- `pane-border-status top` + `pane-border-format` 설정
- **⚠️ 모델/effort 자동 적용 (필수):** `settings.json` `stage_assignments`에서 팀/stage별 모델+effort 읽어 `claude` 기동 시 CLI 플래그로 자동 주입. `start_claude_team.sh`도 동일하게 수정. 현재 두 스크립트 모두 모델/effort 미전달 확인됨 — 이번 릴리스에서 반드시 해결.

---

## 실행 순서

**S1.** Orc-066-dev 세션 spawn (4 panes + 페르소나: 공기성/카더가든/최우영/현봉식)

**S2.** Round A 병렬 진행 (#1 + #2 + #6) — A 패턴 (drafter → reviewer 직접 수정 → finalizer 마감 doc)  
각 의제 별도 commit. drafter/reviewer 반드시 분리 commit.

**S3.** #4 (settings.json schema v0.5) — A 패턴

**S4.** #5 (spawn_team.sh 보강) — #4 완료 후 진입, A 패턴

**S5.** 전체 검증 + CHANGELOG.md v0.6.6 항목 작성

**S6.** 📡 status COMPLETE 출력 → 회의창 보고

---

## 중단 조건

- hook_stop.sh 연결 후 completion_signal.sh 실제 발화 FAIL → 📡 WAITING
- settings.json schema 변경 후 ai_step.sh 기존 호환 FAIL → 📡 WAITING

---

## Critical files

```
docs/r3_active_archive_plan.md          ← #1 상세 권고
scripts/hook_stop.sh                    ← #2 수정 대상
scripts/completion_signal.sh            ← #2 연결 대상
.claude/settings.json                   ← #2 #4 수정 대상
scripts/spawn_team.sh (또는 동일 기능 스크립트) ← #5 수정 대상
handoffs/active/HANDOFF_v0.6.6.md       ← 진행 상태 갱신
```

---

## 자율 영역

- 의제별 commit 메시지 본문
- A 패턴 체크리스트 문서 포맷 (#6 산출)
- 하위호환 처리 방식 세부 (#4)
- pane persona 자동화 구현 방식 (#5)

---

## Orc-066-dev pane 헌법 (필수)

- 4 panes: 공기성(PL, 왼쪽 큰 pane) / 카더가든(drafter) / 최우영(reviewer) / 현봉식(finalizer) 오른쪽 stack
- `select-pane -T <페르소나명>` 박음
- Agent tool 분담 X — tmux split panes 모델 고수

---

**완료 시그널:** `📡 status: COMPLETE — v0.6.6 Standard` + commit trail
