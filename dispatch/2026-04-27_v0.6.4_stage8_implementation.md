# v0.6.4 Stage 8 Implementation dispatch

> **상위:** `docs/03_design/v0.6.4_design_final.md` (commit `8fbbfed`, Score 95, verdict GO, boundary 6/6)
> **본 dispatch:** Stage 8 구현 — M1 → M2 → (M3 ∥ M4) → M5 권장 순서, 공기성 PL 자율 단계 진행
> **하위:** Stage 9 코드 리뷰 → Stage 10 디버그 → Stage 12 QA → Stage 13 release

## 결정

Stage 8 구현 진입. M1~M5 권장 순서 (M1 → M2 → (M3 ∥ M4) → M5). 본 dispatch = 큰 묶음, 공기성 PL이 자율 단계 진행 + **마일스톤 별 commit 분리** 강제. **A 패턴 적용** (drafter 코드 → reviewer 검토+수정 → finalizer 마감 doc, finalizer 본문 작성 X).

## 컨텍스트

- **상위 산출:** `docs/03_design/v0.6.4_design_final.md` (commit `8fbbfed`, 441줄, Score 95, verdict GO)
- **planning_index** (commit `7c139b6`, F-D 4건 + Q1~Q5 5/5 + Stage 5 이월 14건)
- **boundary 6/6 본문** (design_final 박힘): 색상 11종 textual CSS App{} + margin·padding + border round + 진행률 바 8칸 4단계 + 스파크라인 8칸 8단계 + placeholder 3종
- **F-D3 19명 산식:** 박스 16 (4팀 15 + PM status bar 1) + 미표시 placeholder 3 (CTO·CEO·HR) = 조직 19
- **F-D1 PersonaState 단일 spec / F-D2 dashboard.py 단일 진입 / F-D4 sync 시작 (on_mount)** — design_final 본문 박힘
- **운영자 결정 5/5:** Q1=F-D3 19명 / Q2=정확 hook +8h / Q3=osascript 기본·Pushover 회피 / Q4=Windows P1 skeleton / Q5=idle 통합
- **모드 / has_ui / risk:** Strict / yes / medium
- **모델 (`.claude/settings.json` `stage_assignments.stage8_impl`):** claude / Sonnet medium 기본 + Opus high 영역 (PL/reviewer)

## 산출물

- **본 코드:**
  - `scripts/dashboard.py` 단일 진입점 (F-D2)
  - `scripts/dashboard/<module>.py` 모듈 패키지 (예: `data.py` / `render.py` / `pending.py` / `notifier.py` / `personas.py`)
  - `.claude/commands/dashboard.md` 슬래시 정의
- **테스트:** `tests/test_dashboard*.py`
- **마감 doc 5개 (finalizer 영역, 각 ≤ 500줄):**
  - `docs/04_implementation_v0.6.4/m1_scaffold.md`
  - `docs/04_implementation_v0.6.4/m2_data.md`
  - `docs/04_implementation_v0.6.4/m3_render.md`
  - `docs/04_implementation_v0.6.4/m4_pending_notif.md`
  - `docs/04_implementation_v0.6.4/m5_windows_personas.md`

## 핵심 작업 (M1 → M2 → (M3 ∥ M4) → M5)

### M1: scaffold + `/dashboard` 슬래시 (cold start, 외부 의존 0)

- `scripts/dashboard.py` 단일 진입점 (`requirements.txt`에 textual 의존 추가)
- `.claude/commands/dashboard.md` 슬래시 정의 (jOneFlow M-Slash wrapper 위에)
- textual `App{}` scaffold + `on_mount()` 동기 훅 (F-D4)
- commit 분리: `impl(v0.6.4): Stage 8 M1 scaffold — ...`

### M2: 데이터 layer (F-D1 본문)

- `PersonaState` dataclass 단일 spec (M2 module)
- `PersonaDataCollector` (M2) + `PendingDataCollector` (M4 영역, 분리 SRP)
- 토큰량 정확 hook (Q2 +8h) — claude CLI hook 또는 등가
- idle 통합 (Q5) — tmux 미존재 = idle
- commit 분리

### M3: 렌더 (boundary 6건 본문)

- 박스 3개 (기획 / 디자인 / 개발) + PM 상단 status bar 1행 (F-D3)
- 페르소나 행 = 이름 + 상태(◉/○) + 작업명 + 토큰 + 진행률 바
- 다중 버전 동시 표시 (박스 안 행)
- textual CSS `App{}` 셀렉터 + 색상 11종 + margin·padding + border round + 진행률 바 8칸 4단계 + 스파크라인 8칸 8단계 + placeholder
- commit 분리

### M4: Pending + osascript 알림 (병렬 with M3)

- Pending Push/Commit 박스 + Pending Q 박스
- `osascript -e 'display notification ...'` (Q3, Pushover 회피)
- dedupe 5분 TTL (R-N R-11 정정 = lru_cache → dict+TTL)
- read-only 정책 영구 (F-X-2)
- commit 분리

### M5: Windows skeleton + 18명 매핑 detail

- Windows plyer or win10toast (Q4 P1 skeleton, macOS osascript fallback)
- **18명 매핑 detail (F-X-6 blocking 시점 = 본 stage 진입)** — `personas_18.md` 작성 필요 (없으면 본 stage 시작 시 우선 작성)
- commit 분리

## 분담 (Orc-064-dev 4 panes 헌법, A 패턴 적용)

```
┌─────────────────┬─────────────────────────┐
│                 │ 카더가든-be-drafter      │
│   공기성-개발PL │ (코드 + 테스트 초안 ≤800)│
│   (큰 pane)     ├─────────────────────────┤
│   :1.1          │ 최우영-be-reviewer       │
│                 │ (검토+수정 R-N 본문 ≤600)│
│   팀 지휘 +     ├─────────────────────────┤
│   verdict +     │ 현봉식-be-finalizer      │
│   commit        │ (마감 doc만 ≤500)        │
└─────────────────┴─────────────────────────┘
```

| pane | 페르소나 | 본분 | 산출 |
|------|---------|------|------|
| 1.1 공기성-개발PL | Opus high | 팀 지휘 + 단계 진행 + verdict + git_checkpoint commit + bridge-064 보고 | M1~M5 단계별 verdict trail |
| 1.2 카더가든-be-drafter | Haiku medium | M별 코드 + 테스트 초안 작성 (≤ 800줄) | 본 코드 (`scripts/dashboard*.py` + `tests/`) |
| 1.3 최우영-be-reviewer | Opus high | **검토 + 본인이 직접 수정** (R-N 본문 박음, ≤ 600줄) — A 패턴 | 코드 정정 + R-N trail |
| 1.4 현봉식-be-finalizer | Sonnet medium | 마감 doc만 작성 (verdict + Score + AC + 결정 trail, ≤ 500줄, **본문 작성 X**) | `m1_scaffold.md` ~ `m5_windows_personas.md` 5개 |

**A 패턴 강제 (헌법 사고 14):** drafter 초안1 → reviewer 검토 + **본인이 직접 수정** → finalizer 마감 doc만. finalizer가 본문 작성 X. verbatim 흡수 X. 산출물 길이 임계 PASS.

## 보고 형식

각 마일스톤 완료 시 공기성 PL이 bridge-064에 시그니처 1줄 (wrap 회피):

```
📡 status v0.6.4 Stage 8 M<N> COMPLETE — path=<file> tests=<pass>/<total> AC=자동N/수동M Score=N/100 commit=<SHA>
```

전체 Stage 8 완료 시:

```
📡 status v0.6.4 Stage 8 IMPL COMPLETE — M1~M5 done total commits=<N> Score=N/100 verdict=GO → Stage 9 진입
```

중간 진행 운영자 보고 X — bridge-064가 시그니처 한정 + 산출 mtime 이중 polling으로 캐치, 회의창이 commit SHA + 산출 경로만 운영자에 보고.

## 중단 조건 (즉시 공기성 → bridge-064 → 회의창 보고)

- **DEFCON 발견** (`git push` / 외부 API / `rm -rf` / `git reset --hard` / 보안 위반) → 즉시 운영자 게이트
- M2 PersonaState 인터페이스 spec 결정 영역 외 발생
- 산출물 길이 임계 초과 시도 (drafter 800 / review 600 / final 500 초과) → 압축 또는 sub-doc 분할
- AC 자동 비율 < 70%
- Score < 80%
- 헌법 위반 (Orc 안 split 4 panes 미준수 / 페르소나 이름 미표시 / Agent tool 분담 / claude CLI 수동·대화형 기동 / settings.json 위험 키 추가)
- 사고 12 / 13 / 14 재발

## critical files (필독)

- **상위 산출:**
  - `docs/03_design/v0.6.4_design_final.md` (commit `8fbbfed`, 441줄) — boundary 6건 + F-D 본문
  - `docs/02_planning_v0.6.4/planning_index.md` (commit `7c139b6`)
  - `docs/02_planning_v0.6.4/planning_01_M1_scaffold.md` ~ `planning_05_M5_windows_personas.md` (5건)
  - `docs/01_brainstorm_v0.6.4/brainstorm.md`
- **운영 정책:**
  - `docs/operating_manual.md` Sec.5 Stage 플로우
  - `docs/bridge_protocol.md` (★ Sec.4 표 dispatch 작성 정책 + Sec.6 사고 14 = drafter v2 / verbatim 흡수 / 본분 역전 헌법 박힘)
  - `CLAUDE.md`

## 자율 영역 (백앤드 트리오 + 공기성 PL)

- 모듈 디렉토리 구조 detail
- dataclass 필드 / 타입 / 직렬화 형식
- `on_mount()` 동기 훅 + textual App lifecycle 정합성
- 토큰량 정확 hook 인터페이스 spec (Q2 +8h)
- osascript 명령 형식 (Q3)
- AC 표 자동화 명령 1줄 / verbatim 마커 위치
- commit msg 표현 / commit 분리 단위 (회의창 작성 영역 X)
- Score 가중치 분배

## Stage 8 → Stage 9 진입 게이트 (자율 모드)

- M1~M5 5개 마일스톤 모두 commit 분리
- AC 자동 비율 ≥ 70% (Stage 5/6/7 71% 동급)
- 테스트 PASS
- Score ≥ 80% (목표 90%+)
- F-D 본문 흡수 (D1 PersonaState / D2 dashboard.py / D4 sync)
- Q1~Q5 본문 흡수 (인터페이스 spec 형식)
- boundary 6건 본문 (M3)
- 18명 매핑 detail (M5, `personas_18.md` 도착 후 — 본 stage 진입 시 작성 필요)
- 산출물 길이 임계 준수 (drafter ≤ 800 / review ≤ 600 / final ≤ 500)
- 운영자 결정 영역 0건 (DEFCON 외 자율, 발생 시 즉시 보고)
- 헌법 위반 0건 (자가 점검 9항목 + 사고 13/14 회피)
- → 통과 시 공기성 PL이 bridge-064 → 회의창 → 운영자에 commit SHA + 산출 + Stage 9 진입 시그널

---

작성: 회의창 (CTO 실장 백현진, 세션 28)
실행: Orc-064-dev (공기성 개발PL + 카더가든·최우영·현봉식 백앤드 트리오) — 4 panes 모두 부팅 검증 완료
모드: Strict / has_ui=yes / risk=medium
모델: claude (settings.json) / Sonnet medium 기본 + Opus high (PL / reviewer)
정책: A 패턴 + dispatch 무게 정책 + 헌법 사고 13/14 적용 (v0.6.5 spec v2 미리보기)
