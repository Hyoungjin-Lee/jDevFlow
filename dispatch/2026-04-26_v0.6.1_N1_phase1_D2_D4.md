# 업무지시 — v0.6.1 N1 phase1 마무리 (D2 + D4 병행)

> 회의창(브릿지) → 에이전트팀 오케스트레이터 dispatch.
> 본문 / commit msg / 검증 절차 step / 헬퍼 시그니처 등 구체 본문은 오케 자율 판단 영역.

## 작업 위치
`/Users/geenya/projects/Jonelab_Platform/jDevFlow`

## 컨텍스트 읽기 순서
1. `CLAUDE.md` (Sec.2.5 조직도 + Sec.3 승인 정책)
2. `HANDOFF.md` Status (세션 24 D3 보류 결과)
3. `/tmp/jdevflow_n1_status.md` (세션 24 phase1 산출물 상세)
4. `CHANGELOG.md` [Unreleased]

## 세션 24 결과 요약
- main HEAD: `759aeb1` ← `27d886a` (filter-repo 적용) ← `afcea6a` ← `b01fac0`
- backup 2개 영구 보존: `backup-pre-v0.6.1-rename` (afcea6a), `backup/pre-n1-20260426-030410` (b01fac0)
- 잔재 2건: AC-N1-3 진짜 1건 (v0.1 init commit msg) + AC-N1-1 false-positive 706 hits (자기-치환)
- push 0회

## 운영자 결정 (Q1~Q4)

| Q | 결정 |
|---|---|
| Q1 AC-N1-3 | `verify_all.sh`에 v0.1 init commit `19b95859` whitelist 1줄 추가 (D4 patch에 묶음) |
| Q2 GitHub rename | `gh auth status` OK면 `gh repo rename` 자동, 안 되면 Web UI 안내 |
| Q3 `expressions.txt` → `.md` | phase2 완료 후 별도 commit |
| Q4 백업 브랜치 원격 push | `backup-pre-v0.6.1-rename` + `backup/pre-n1-20260426-030410` 둘 다 push |

## 해야 할 일 (순서)

- **S1.** D4 patch — `post_check.sh` / `verify_all.sh` / `pre_check.sh` 자기-치환 회피 + `verify_all.sh`에 Q1 whitelist 1줄. shellcheck PASS + `verify_all.sh phase1` 재실행 PASS 확인 후 commit 1건.
- **S2.** GitHub repo rename — `gh auth status` 확인 → 가능하면 `gh repo rename jOneFlow --repo Hyoungjin-Lee/jDevFlow --yes`, 아니면 Web UI 안내. 운영자 1회 "go" 시그널 받기.
- **S3.** `bash scripts/v0.6.1/rename_n1.sh phase2 --apply --yes` 실행 (verify phase2 PASS).
- **S4.** 운영자 명시 승인 후 `git push --force origin main` + 백업 브랜치 2개 push.
- **S5.** 운영자 폴더 mv (`jDevFlow` → `jOneFlow`) 완료 시그널 후 새 폴더에서 `verify_all.sh phase3`.
- **S6.** `expressions.txt` → `expressions.md` git mv + 참조 업데이트 commit.
- **S7.** `HANDOFF.md` / `CHANGELOG.md` `[Unreleased]` → `[v0.6.1]` 승격 commit + 운영자 승인 후 push.

## 보고 / 중단 정책

- 각 stage 완료 시 **회의창에 자발적 보고** (capture-pane + status 종합).
- 중단 조건 (즉시 회의창 보고 + 자동 재시도 1회만):
  - shellcheck FAIL
  - phase1 재검증에서 AC-N1-1 ≠ 0 hits
  - `ls-remote` 실패
  - mv 후 git status dirty 또는 `.git` 부재
- 운영자 명시 승인 영역 (CLAUDE.md Sec.3): S2 rename 트리거 / S4 push / S5 mv.

## Critical files
- `scripts/v0.6.1/post_check.sh` (D4 patch)
- `scripts/v0.6.1/verify_all.sh` (D4 patch + Q1 whitelist)
- `scripts/v0.6.1/pre_check.sh` (D4 patch)
- `scripts/v0.6.1/rename_n1.sh` (손대지 않음)
- `scripts/v0.6.1/expressions.txt` → `expressions.md` (S6)
- `HANDOFF.md`, `CHANGELOG.md` (S7)

## 자율 판단 영역
- D4 헬퍼 함수 위치 (3 스크립트 inline 복제 vs 외부 파일)
- D4 인코딩 옵션 (base64 / printf 16진수 / 외부 파일)
- commit 분리 정책 (단일 vs 파일별)
- whitelist 1줄 정확한 위치 (`_ac_n1_3` 함수 안)
- shellcheck disable 추가 여부
