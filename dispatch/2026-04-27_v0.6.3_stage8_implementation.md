# v0.6.3 Stage 8 Implementation dispatch

## 결정
v0.6.3 Stage 8 구현 — M1+M4 critical path 병렬 + M2/M3 순차 + M5 conditional 인터페이스만 (운영자 결정 #15 흡수). 자율 모드 (Stage 5~12 박지영 자율 + 4 pane 병렬). 속도 우선 — drafter 동시 + reviewer/finalizer 폴링 (운영자 재촉 명시).

## 컨텍스트
- v0.6.3 Stage 5 완료 (design_final.md, Score ≥ 80% Stage 8 GO)
- F-62-5~F-62-9 5건 결정 흡수 (technical_design 본문 반영)
- Q1~Q4/Q6/Q-NEW-3 해소 완료
- 5월 초 Windows 마감 — M1+M4 critical path 우선

## 산출물
- **M1** (✅ critical): `scripts/monitor_bridge.sh` 신규 / `scripts/update_handoff.sh` 신규 / `scripts/setup_tmux_layout.sh` 개정
- **M2** (🟡): `~/.claude/CLAUDE.md` 보편 정책 (충돌 우선순위 F-62-5)
- **M3** (🟡): `.claude/settings.json` hooks.PostToolUse + `docs/guides/ethos_checklist.md`
- **M4** (✅ critical): 18명 spawn 자동화 sketch + 리뷰어 conditional 분기 로직 (인터페이스만, F-62-8)
- **M5** (🟢): `scripts/ai_step.sh` Stage 9 분기 (conditional 인터페이스만, plugin-cc 후속)

## 핵심 작업
- 4 panes 병렬 구현 (drafter 동시 + reviewer/finalizer 폴링 패턴)
- AC 검증 자동화: `shellcheck` CLEAN / `python3 -m py_compile` / `grep`/`wc -l` 패턴
- 각 모듈 단위 commit (git_checkpoint.sh, push 미진행)
- Stage 9 미리 코드 리뷰 sketch 가능 (1.3 최우영 자율)

## 분담 (Orc-063-dev 4 panes 유지)
- **1.1 공기성-dev-PL** (Opus high) — 분담 + 진행 모니터링 + Stage 9 진입 게이트 + 박지영 보고
- **1.2 카더가든-be-draft** (Haiku medium) — M1+M4 백엔드 모듈 병렬 구현 (critical path)
- **1.3 최우영-be-reviewer** (Opus high) — Stage 9 코드 리뷰 sketch (구현 진행 중 병렬)
- **1.4 현봉식-be-finalizer** (Sonnet medium) — M2/M3/M5 통합 + AC 검증 + 회귀 테스트

## 중단 조건 (즉시 박지영 Orc-063-plan:1.1 보고)
- shellcheck FAIL → 즉시 fix loop (자율) / 3회 실패 시 박지영 보고
- py_compile FAIL → 즉시 fix
- AC 미달 / 위험 명령 정책 위반 → 즉시 박지영 보고
- ~/.claude/CLAUDE.md 작성 시 기존 백업 + 충돌 검증

## 보고
- 각 M 단위 commit 후 박지영(1.1) capture 보고 (📡 commit SHA + AC 통과)
- Stage 8 종합 완료 시 📡 status: Stage 8 implementation — commit chain + AC <통과/전체> + 다음 stage = Stage 9
- 자율 모드 운영자 보고 X (Stage 13만)

## critical files
- **신규**: `scripts/monitor_bridge.sh`, `scripts/update_handoff.sh`, `~/.claude/CLAUDE.md`, `docs/guides/ethos_checklist.md`
- **개정**: `scripts/setup_tmux_layout.sh`, `.claude/settings.json`, `scripts/ai_step.sh`
- **참조 (필독)**: `docs/03_design/v0.6.3_design_final.md`, `docs/03_design/v0.6.3_technical_design.md`, `docs/02_planning_v0.6.3/plan_final.md`

## 자율 영역
- 모듈 분담 디테일 / 구현 순서 / 헬퍼 함수 위치 / 시그니처
- 단위 테스트 / 회귀 테스트 패턴
- commit 묶음 단위 / msg 디테일
- AC 자동 검증 우선, 수동 확인 5건 이내 유지

## 위험 명령 정책
- `~/.claude/CLAUDE.md` 작성 = 글로벌 영역 = 백업 + 충돌 우선순위 검증 필수 (F-62-5)
- push 미진행 (Stage 13 운영자 게이트만)
- BSD/GNU sed 분기 (F-62-6 — `update_handoff.sh`)

## Stage 8 → Stage 9 진입 게이트 (자율 판정)
- AC 통과 ≥ 19/25 (자동 검증 가능)
- shellcheck CLEAN 전수 / py_compile PASS 전수
- F-62-x 9건 흡수 (F-62-10 본 stage 결정 시 추가)
- → 통과 시 박지영이 Stage 9 brief 발행 (1.3 최우영 sketch 활용)
