# Stage 12 brief — 현봉식 (be-finalizer, Sonnet medium, Orc-063-dev:1.4)

**상위 dispatch:** `dispatch/2026-04-27_v0.6.3_stage12_qa.md`
**상위 input:** `docs/05_qa/v0.6.3_stage12_manual_qa.md` (본인이 사전 작성, 5건 시나리오)
**참조:** `docs/04_implementation_v0.6.3/code_review.md` (Score 89.75% APPROVED), `docs/03_design/v0.6.3_design_final.md`
**산출물:** `docs/05_qa_v0.6.3/qa_report.md` (수동 5건 PASS/FAIL + 회귀 21건 재실행 + Score 재산정)

---

## 결정

(b) Stage 12 QA 본격 — 수동 5건 직접 판정 + 자동 21건 회귀 재실행 + Score 재산정 임계 80%. 자율 모드 / 운영자 보고 X / 속도 우선 thinking medium.

## 핵심 작업

### 1. 수동 QA 5건 PASS/FAIL 직접 판정

`docs/05_qa/v0.6.3_stage12_manual_qa.md` 시나리오대로 직접 실행 + 결과 기록:

| ID | 시나리오 | 검증 명령/방법 | PASS/FAIL |
|----|---------|------------|----------|
| MQ-M1-1 | Monitor 재시작 후 신호 정상 캐치 | tmux 세션 spawn → monitor_bridge.sh 실행 → 신호 emit 확인 | ⏳ |
| MQ-M2-1 | ~/.claude/CLAUDE.md draft 품질 (모순 0건) | 본문 grep + 충돌 우선순위 일관성 검증 | ⏳ |
| MQ-M4-1 | tmux 1+3 pane 동시 spawn 안정성 | spawn_team.sh 실행 → pane 4개 + title 확인 | ⏳ |
| MQ-M4-2 | 리뷰어 conditional 환경 분기 동작 | ai_step.sh Stage 9 분기 → "Codex review 경로" 또는 "self-review fallback" 출력 검증 | ⏳ |
| MQ-M5-1 | Codex 자동 호출 vs fallback 동작 | command -v codex → 환경 분기 확인 + fallback 메시지 검증 | ⏳ |

각 항목 **PASS** 시: 검증 명령 출력 + 정합 근거 명시
**FAIL** 시: 실패 사유 + Stage 10 패치 권고 또는 v0.6.4 이월 권고

### 2. 자동 AC 21건 회귀 재실행

```bash
bash tests/v0.6.3/run_ac_v063.sh
# 결과 = 21/21 (100%) 또는 회귀 N건 — 회귀 0 PASS 임계
```

(1.2 Stage 10 fix commit 후 실행 권장 — 1.2 마커 도달 폴링 후 진입)

### 3. Score 재산정 (Stage 12 QA 자체)

Stage 12 가중치 (자율 영역 — design_final 패턴 참고):

| 항목 | 가중치 | 기준 |
|------|-------|------|
| (1) 수동 QA 통과율 | 35% | 5/5 PASS = 100점, 1건 FAIL당 -20점 |
| (2) 자동 AC 회귀 | 25% | 21/21 PASS = 100점, 회귀 1건당 -10점 |
| (3) shellcheck 회귀 | 15% | error 0 = 100점 |
| (4) 보강 12건 흡수 | 15% | Stage10/12 분류 흡수 + v0.6.4 이월 trace 명료성 |
| (5) Stage 13 release 준비도 | 10% | release_v0.6.3.sh 사전 작성 + Gate 1~7 검증 |
| **합계** | **100%** | 임계 80% |

### 4. Stage 13 진입 게이트 판정

| 조건 | 기준 | 결과 |
|------|------|------|
| Score | ≥ 80% | ⏳ |
| 수동 5/5 PASS | 5/5 | ⏳ |
| AC 회귀 0건 | 21/21 | ⏳ |
| 운영자 결정 영역 0건 | 0건 | ⏳ |

→ 4건 모두 PASS 시 Stage 13 진입 GO 판정.

## 본문 골격

```
---
version: v0.6.3
stage: 12 (qa_report)
date: 2026-04-27
authored_by: 현봉식 (개발팀 백엔드 파이널라이저, Sonnet/medium, Orc-063-dev:1.4)
upstream:
  - docs/05_qa/v0.6.3_stage12_manual_qa.md (사전 시나리오 5건)
  - docs/04_implementation_v0.6.3/code_review.md (commit 5b3ead3)
  - tests/v0.6.3/run_ac_v063.sh (자동 21건)
status: qa_final
session: 27
---

# Sec. 0. QA 개요 + 입력 산출물 trace
# Sec. 1. 수동 QA 5건 PASS/FAIL 판정 (직접 실행 결과)
# Sec. 2. 자동 AC 21건 회귀 결과
# Sec. 3. shellcheck/py_compile 회귀
# Sec. 4. Score 재산정 (5항목 × 가중치)
# Sec. 5. Stage 13 진입 게이트 판정
# Sec. 6. APPROVED/REJECTED 종합 verdict
```

## 작업 지시 + 마커

1. 1.2 Stage 10 fix commit 도달 후 진입 (폴링 또는 stand-by)
2. 수동 5건 직접 실행 + 자동 21건 회귀 → 결과 표 기록
3. Score 재산정 + Stage 13 게이트 판정
4. 마지막 라인: `COMPLETE-FINALIZER-S12: verdict=APPROVED|REJECTED, score=<X>%, 수동=<통과>/5, AC=<통과>/21, gate=PASS|FAIL, file=docs/05_qa_v0.6.3/qa_report.md`

## 자율 영역

- qa_report.md 형식 (자유, design_final 패턴 참고 가능)
- Stage 12 Score 가중치 (위 안 자율 조정 OK)
- 수동 시나리오 검증 명령 디테일

## 중단 조건

- 수동 FAIL 발생 → 본인(공기성 1.1) 즉시 보고 (Stage 10 패치 또는 v0.6.4 이월 권고 포함)
- AC 회귀 발생 → 즉시 보고
- Score < 80% → 즉시 보고

## 보고

`COMPLETE-FINALIZER-S12` 마커. 운영자 보고 X.
