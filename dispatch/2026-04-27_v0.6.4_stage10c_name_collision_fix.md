---
date: 2026-04-27
version: v0.6.4
stage: 10c (followup)
work: dashboard.py 이름 충돌 fix + __init__.py + 진입 명령 정정
prev: Stage 10b commit a891d97 (M2~M5 wiring 통합 APPROVED Score 93/100, 단 import 경로 깨짐)
trigger: 운영자가 venv/bin/python3 scripts/dashboard.py 실행 → ModuleNotFoundError: No module named 'scripts'
---

# Stage 10c brief — dashboard.py 이름 충돌 fix

> brief만. 본문 / 코드 / commit msg / 검증 step = PL 자율.

## 1. 운영자 결정

**(가)** `dashboard.py` → `run_dashboard.py` 이름 변경 + `scripts/__init__.py` 추가 + `.claude/commands/dashboard.md` 진입 명령 정정.

## 2. 컨텍스트

- `scripts/dashboard.py` (파일) ↔ `scripts/dashboard/` (디렉토리, 패키지) 같은 이름 = Python import 충돌
- `scripts/__init__.py` 누락 = `scripts.*` 절대 import 동작 X
- Stage 10b PL(공기성)이 wiring 코드 작성 후 **운영자 실제 실행 검증 0건** = Score 93/100 APPROVED인데 `ModuleNotFoundError` (Stage 9 패턴 반복: 점수만 통과 / 진짜 동작 검증 누락)

## 3. 자율 영역 (PL 자율)

- 파일명 변경 (`scripts/dashboard.py` → `scripts/run_dashboard.py`)
- `scripts/__init__.py` 추가 (빈 파일 또는 docstring만)
- `.claude/commands/dashboard.md` 진입 명령 정정 (`scripts/dashboard.py` → `scripts/run_dashboard.py`)
- 기타 진입 경로 참조가 들어 있는 위치 정합 (CHANGELOG / docs / dispatch 본문 등)
- 팀 분담 / commit msg 본문 / 검증 step

## 4. 검증 게이트 (강제, release blocker 닫는 기준)

**다음 명령이 운영자 화면에 본문 표시 PASS:**
```bash
cd /Users/geenya/projects/Jonelab_Platform/jOneFlow
venv/bin/python3 scripts/run_dashboard.py
```

본문 = 19명 박스 + 진행률 바 + 스파크라인 + Pending 박스 표시. M1 placeholder X.

**PL이 직접 실행해서 본문 표시 확인 후에만 commit + flag.** 운영자 실행 검증 미수행 = Stage 10c FAIL (Stage 9 / Stage 10b 패턴 반복 회피).

## 5. 중단 조건 (DEFCON)

git push / 외부 API / rm -rf / 비복구 손실 / 보안 위반 / test FAIL / shellcheck 빨간줄 / py_compile 실패 / 운영자 결정 필요 부분.

## 6. critical files

- `scripts/dashboard.py` → `scripts/run_dashboard.py` (이름 변경 대상)
- `scripts/__init__.py` (신규)
- `.claude/commands/dashboard.md` (진입 명령 정정)
- 기타 진입 경로 참조 (PL이 grep으로 찾을 위치)

## 7. A 패턴 강제

drafter ≤ 800 / reviewer ≤ 600 (drafter 본문 직접 수정) / finalizer ≤ 500 (본문 작성 X). drafter v2 X. verbatim 흡수 X. 분량 작은 작업이라 마일스톤 분할 X.

## 8. 헌법 포인터

- A 패턴 = `bridge_protocol.md` Sec.4 표 + Sec.6 사고 14
- 부팅 검증 = Sec.8 11항
- 3중 검증 = Sec.8 10항
- push 정공법 = `.skills/push-signal-watcher/SKILL.md`

## 9. 산출

- finalizer 마감 doc = `docs/04_implementation_v0.6.4/stage10c_final.md` (PL 자율 명명)
- commit = `git_checkpoint.sh "Stage 10c dashboard.py 이름 충돌 fix" <files>` (push X)
- 완료 flag = `handoffs/active/v0.6.4/stage10c_complete.flag` (시그니처 1줄)
- 결정 / 중단 시 = `stage10c_decision.flag` / `stage10c_abort.flag`

---

**brief 끝.** PL 자율 + A 패턴 + push 정공법 + **운영자 실행 검증 게이트 강제**.
