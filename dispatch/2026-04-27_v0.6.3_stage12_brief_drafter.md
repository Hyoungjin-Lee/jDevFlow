# Stage 12 brief — 카더가든 (be-draft, Haiku medium, Orc-063-dev:1.2)

**상위 dispatch:** `dispatch/2026-04-27_v0.6.3_stage12_qa.md`
**선례 패턴:** `scripts/release_v0.6.2.sh` (Gate 1~7 + dry-run/--confirm)
**상위 input:** code_review.md 보강 12건 (Stage10=1 / Stage12=4 / v0.6.4 이월=7), commit 5b3ead3
**산출물:** (d) `scripts/release_v0.6.3.sh` + (a) Stage 10 P1 fix

---

## 결정

(d) Stage 13 release 스크립트 사전 작성 + (a) Stage 10 보강 1건 자율 fix (Codex P1 검출). 자율 모드 / 운영자 보고 X / 속도 우선 thinking medium.

## 핵심 작업 (a) Stage 10 P1 fix — `scripts/ai_step.sh` Stage 9 fail-safe 연결

**Codex 검출 (codex_response.md, P1 회귀):**
> `stage9_review=codex` 이지만 codex CLI 미발견 환경에서 self-review fallback 헬퍼가 `ai_step_run_next()`에서 호출되지 않음. 현재 실행 경로는 `ai_step_resolve_executor()`→`ai_step_dispatch()`만 타므로 fallback 미연결 → Stage 9 진행 차단 가능.

**fix 패턴:**
```bash
# scripts/ai_step.sh ai_step_run_next() 또는 Stage 9 분기 진입점에서:
if [ "$current_stage" = "9" ]; then
  stage9_reviewer=$(jq -r '.stage_assignments.stage9_review // empty' .claude/settings.json 2>/dev/null)
  [ -z "$stage9_reviewer" ] && stage9_reviewer=$(grep "stage9_review" .claude/settings.json | cut -d'"' -f4)
  if [ "$stage9_reviewer" = "codex" ] && ! command -v codex >/dev/null 2>&1; then
    echo "⚠️ Stage 9: Codex 미감지. self-review fallback 실행 (BR-002 trace)"
    # self-review 분기 헬퍼 호출 (기존 함수 재사용)
  fi
fi
```

ai_step.sh 기존 구조 유지 + fail-safe 분기 1건 추가. shellcheck CLEAN 검증 + AC 21/21 회귀 0건 확인.

## 핵심 작업 (d) `scripts/release_v0.6.3.sh` 사전 작성

**선례 인용:** `scripts/release_v0.6.2.sh` 패턴 계승 — Gate 1~7 + dry-run/--confirm 인터페이스.

**Gate 항목 (v0.6.3 적응):**
- Gate 1: working tree clean (`git status --short` empty 검증)
- Gate 2: 현 브랜치 = main + commit chain 도달 (5ac2e16/868de75/ea2fd16/5b3ead3 + Stage 12 QA commit)
- Gate 3: AC 자동 21/21 PASS (`bash tests/v0.6.3/run_ac_v063.sh`)
- Gate 4: shellcheck CLEAN (info-level 허용, error 0건)
- Gate 5: 수동 QA 5/5 PASS (qa_report.md verdict 확인)
- Gate 6: BR 목록 확인 (BR-001 + BR-002 + 신규 BR — Stage 13 운영자 노출 영역)
- Gate 7: tag 발행 직전 — `git tag v0.6.3` (--confirm 시만 실행)

**dry-run 패턴:**
```bash
[[ "${1:-}" == "--dry-run" ]] && DRY_RUN=1 || DRY_RUN=0
[[ "${1:-}" == "--confirm" ]] && CONFIRM=1 || CONFIRM=0
```

**push X (Stage 13 운영자 게이트만):** 본 스크립트는 tag 생성까지. push는 운영자 명시 승인 후 별도 명령.

## 작업 지시 + 마커

1. (a) Stage 10 fix 먼저 적용 → AC 회귀 재검증 → commit (msg: "fix(v0.6.3): Stage 10 P1 — ai_step.sh Stage 9 fail-safe 연결 (Codex P1 검출)")
2. (d) release_v0.6.3.sh 작성 → shellcheck CLEAN
3. 마지막 라인: `COMPLETE-DRAFTER-S12: Stage10_fix=완료, release_v063=<줄수>줄, AC_회귀=<통과>/<전체>, shellcheck=CLEAN`

## 자율 영역

- ai_step.sh fix 위치 / 헬퍼 함수 시그니처
- release.sh Gate 표현 / dry-run 출력 형식
- commit 단위 (1건 또는 2건 묶음)

## 중단 조건

- AC 회귀 발생 → 본인(공기성 1.1) 즉시 보고
- shellcheck error 발생 → 자가 fix 후 재시도, 3회 실패 → 보고
- 위험 명령 직접 실행 → 즉시 중단

## 보고

`COMPLETE-DRAFTER-S12` 마커. 운영자 보고 X (자율 모드, Stage 13만).
