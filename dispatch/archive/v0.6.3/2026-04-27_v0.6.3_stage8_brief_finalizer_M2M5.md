# Stage 8 brief — 현봉식 (be-finalizer, Sonnet medium, Orc-063-dev:1.4)

**상위 dispatch:** `dispatch/2026-04-27_v0.6.3_stage8_implementation.md`
**상위 input:** `docs/03_design/v0.6.3_design_final.md`
**참조:** `docs/03_design/v0.6.3_technical_design.md` (M2/M5 detail)
**산출물:** M2/M5 통합 + AC 검증 자동화 + 회귀 테스트

---

## 결정

M2(글로벌 ~/.claude sketch) + M5(Stage 9 Codex conditional 인터페이스) 통합 + 전체 AC 21건 자동 검증 스크립트. 백실장 정정: thinking medium / 속도 우선 / Q 추천 자동.

## 컨텍스트

- 자율 모드 — 운영자 보고 X
- 1.2(M1+M4) + 1.3(M3) 병렬 진행, 본인 동시 진행
- **위험**: ~/.claude/CLAUDE.md 작성 = 글로벌 영역 → **백업 필수** + F-62-5 충돌 우선순위 검증
- push X (Stage 13 게이트)

## 핵심 작업 (M2)

### M2-1. `~/.claude/CLAUDE.md` 신규 작성 (보편 정책 sketch)

**위험 정책 흡수 (필수)**:
1. 기존 파일 존재 시 백업: `[ -f ~/.claude/CLAUDE.md ] && cp ~/.claude/CLAUDE.md ~/.claude/CLAUDE.md.bak.$(date +%Y%m%d)`
2. 작성 후 충돌 우선순위 명시 검증

design_final Sec.3.2 + Sec.5.1 (F-62-5):
- 보편 정책 ≥ 60줄
- 포함: **보안** / **언어** / **톤** / **CLI 자동화** (4개 키워드 명시)
- specificity 원칙 명시: "프로젝트 설정이 글로벌을 override합니다 (specificity 원칙)"
- 프로젝트 특화 X (운영자 매뉴얼 영역은 프로젝트 CLAUDE.md로)

### M2-2. 프로젝트 `CLAUDE.md` 글로벌 포인터 추가

`CLAUDE.md` Sec.2 또는 Sec.3에:
```
> 글로벌 보편 정책 = `~/.claude/CLAUDE.md` 참조 (specificity: 프로젝트 > 글로벌)
```

기존 본문 보존 — 포인터 1줄만 추가.

## 핵심 작업 (M5)

### M5-1. `scripts/ai_step.sh` Stage 9 분기 sketch (1.2 인터페이스 보강)

1.2 카더가든 작업한 stage9_reviewer 분기 인터페이스를 ai_step.sh에 통합:
- Stage 9 진입 분기 함수 (`ai_step_stage9_review_route`)
- Codex path: plugin-cc 호출 sketch (실 구현 후속)
- self-review fallback: 운영자 알림 메시지 출력
- `command -v codex` 감지

### M5-2. CLAUDE.md F-62-9 옵션 강제 명시 추가

CLAUDE.md Sec.4 또는 Sec.5에:
```
모든 claude CLI 자동화 호출은 `--dangerously-skip-permissions` 옵션 강제 (F-62-9)
```

## 핵심 작업 (AC 검증 자동화)

### `tests/v0.6.3/run_ac_v063.sh` 신규 작성

design_final Sec.7.2 자동 AC 21건 일괄 검증:
- 21건 명령 순차 실행
- pass/fail 카운트 출력
- 마지막 라인: `AC v0.6.3: <통과>/<전체> (<퍼센트>%)`

```bash
#!/usr/bin/env bash
pass=0; fail=0; total=21
log() { local r=$1 d=$2; if [ $r -eq 0 ]; then pass=$((pass+1)); echo "✅ $d"; else fail=$((fail+1)); echo "❌ $d"; fi; }

# M1 (5)
[ -f scripts/monitor_bridge.sh ] && [ "$(wc -l < scripts/monitor_bridge.sh)" -ge 30 ]; log $? "M1-1 monitor_bridge.sh ≥30줄"
grep -q "📡 status" scripts/monitor_bridge.sh; log $? "M1-2 시그니처"
grep -q "\-S \-20" scripts/monitor_bridge.sh; log $? "M1-3 capture 범위"
bash scripts/update_handoff.sh --dry-run 2>&1 | grep -qiE "windows|fallback|copy|sync"; log $? "M1-4 windows fallback"
grep -q "date +%Y-%m-%d" scripts/monitor_bridge.sh; log $? "M1-5 timestamp"

# M2 (4)
[ -f ~/.claude/CLAUDE.md ] && [ "$(wc -l < ~/.claude/CLAUDE.md)" -ge 60 ]; log $? "M2-1 ~/.claude/CLAUDE.md ≥60"
[ "$(grep -c "specificity\|프로젝트 > 글로벌" CLAUDE.md)" -ge 1 ]; log $? "M2-2 specificity"
grep -q "~/.claude/CLAUDE.md" CLAUDE.md; log $? "M2-3 글로벌 포인터"
[ "$(grep -c "보안\|언어\|톤\|CLI 자동화" ~/.claude/CLAUDE.md)" -ge 3 ]; log $? "M2-4 보편 정책 4 키워드"

# M3 (5)
[ "$(grep -c "PostToolUse" .claude/settings.json)" -ge 1 ]; log $? "M3-1 hooks.PostToolUse"
grep -q "py_compile" .claude/settings.json; log $? "M3-2 py_compile"
grep -q "shellcheck" .claude/settings.json; log $? "M3-3 shellcheck"
[ -f docs/guides/ethos_checklist.md ]; log $? "M3-4 ethos_checklist"
[ "$(grep -cE "Boil the Lake|autoplan|investigate" docs/guides/ethos_checklist.md)" -ge 3 ]; log $? "M3-5 ETHOS 3종"

# M4 (4)
[ "$(wc -l < docs/02_planning_v0.6.3/personas_18.md)" -ge 150 ]; log $? "M4-1 personas_18 ≥150"
grep "stage9_review" .claude/settings.json | grep -q "codex"; log $? "M4-2 stage9_review codex"
[ "$(grep -A10 "Codex" docs/02_planning_v0.6.3/personas_18.md | wc -l)" -ge 10 ]; log $? "M4-3 Codex 정의"
grep -q "조직도" docs/operating_manual.md; log $? "M4-4 조직도 명시"

# M5 (3)
grep -qE "stage9_reviewer|stage9_review" scripts/ai_step.sh; log $? "M5-1 분기 로직"
grep -q "command -v codex" scripts/ai_step.sh; log $? "M5-2 codex 감지"
[ "$(grep -c "Stage 9" scripts/ai_step.sh)" -ge 1 ]; log $? "M5-3 Stage 9 명시"

pct=$(( pass * 100 / total ))
echo ""
echo "AC v0.6.3: $pass/$total (${pct}%)"
[ $pass -ge 19 ] && echo "✅ Stage 9 진입 게이트 PASS (≥19/21)" || echo "❌ Stage 9 진입 게이트 FAIL (<19/21)"
```

## AC 검증 (자체)

작업 완료 시 본인 직접 실행:
- `bash tests/v0.6.3/run_ac_v063.sh`
- 결과: pass ≥ 19 / 21 → Stage 9 게이트 PASS
- shellcheck CLEAN: `shellcheck tests/v0.6.3/run_ac_v063.sh`

## 작업 지시 + 마커

1. M2 → M5 → AC 검증 스크립트 순차 작성
2. ~/.claude/CLAUDE.md 작성 시 백업 필수
3. AC 검증 직접 실행 → 결과 마커에 포함
4. 마지막 라인: `COMPLETE-FINALIZER-M2M5: globalCLAUDE=<줄수>, projectPointer=완료, ai_step_stage9=완료, AC=<통과>/<전체>, gate=PASS|FAIL`

## 자율 영역

- ~/.claude/CLAUDE.md 보편 정책 표현
- ai_step.sh Stage 9 분기 함수 시그니처
- run_ac_v063.sh 출력 형식

## 중단 조건

- AC 통과 < 19/21 → 본인(공기성 1.1) 보고 + 게이트 FAIL 마킹
- ~/.claude/CLAUDE.md 백업 실패 → 즉시 작업 중단
- 충돌 우선순위 명시 누락 → 자가 정정

## 보고

완료 시 `COMPLETE-FINALIZER-M2M5` 마커 + AC 결과 + 게이트 판정.
