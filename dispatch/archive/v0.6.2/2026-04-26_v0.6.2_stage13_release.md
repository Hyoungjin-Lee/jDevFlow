# v0.6.2 Stage 13 Release dispatch

## 결정
v0.6.2 release 스크립트 작성 → dry-run 보고 → 운영자 `--confirm` 후 실 실행.

## 컨텍스트
- Stage 9 APPROVED PASS / Score 93.8% / HEAD `a6ef4be`
- release 명령: `git push origin main` + `git tag v0.6.2` + `git push --tags`
- **신규 정책 (세션 26 박힘):** 위험 명령 = 스크립트화 후 1줄 실행. 운영자에게 명령줄 paste 금지. 메모리 `feedback_dangerous_commands.md` + `docs/bridge_protocol.md` Sec.3 신규 규칙 참조. **본 release가 정책 첫 적용 사례.**

## 산출물
신규 `scripts/release_v0.6.2.sh`

## 중단 조건
- shellcheck FAIL
- dry-run 검증 실패 (현재 브랜치 / 작업 트리 청결 / 원격 추적 / tag 충돌 확인)
- 운영자 `--confirm` 미수신

## 보고
- dry-run 출력 capture-pane으로 회의창에 자발적 보고
- 운영자 `--confirm` 후 실 실행
- 완료 후 commit SHA + tag + remote URL 노출 확인 보고

## critical files
- 신규: `scripts/release_v0.6.2.sh`
- 참조 패턴: `scripts/git_checkpoint.sh` (기존 안전 스크립트 표준)
- 참조 정책: `docs/bridge_protocol.md` Sec.3 / `~/.claude/projects/-Users-geenya-projects-Jonelab-Platform/memory/feedback_dangerous_commands.md`

## 자율 영역
- 스크립트 본문 / 안전장치 디테일 / 인자 파싱 / abort 메시지
- 멱등성 / 롤백 가능성 / `set -e` / `trap`
- 검증 step 개수 / dry-run 출력 형식
- 본 스크립트가 v0.6.x 시리즈 표준화 자산이라면 v0.6.3 D-x 이월 제안 가능 (선택)
