# v0.6.2 Release Cleanup dispatch

## 결정
v0.6.2 release deploy 단계의 마무리 잔재 정리. v0.6.3 진행에 영향 안 가게 분리 commit.

## 컨텍스트
v0.6.2 release deploy 흐름이 archive까지 진행됐지만 commit이 누락된 상태로 brigde-062/Orc-062 종료됨. git status에 잔재가 남아 v0.6.3 진행에 시각적 혼선.

git status 잔재:
- `D HANDOFF.md` — symlink 끊김 (v0.6.2 archive 후 v0.6.3 신규 미연결)
- `D handoffs/active/HANDOFF_v0.6.2.md` — archive로 mv (정상 동작이나 commit 안 됨)
- `?? handoffs/archive/HANDOFF_v0.6.2.md` — archive에 신규 파일 (untracked)
- `?? dispatch/2026-04-26_v0.6.2_stage13_deploy.md` — archive 안 됨

**별도 잔재 (v0.6.3 영역, 본 dispatch 처리 X):**
- `?? dispatch/2026-04-26_v0.6.3_stage234_planning.md` — v0.6.3 plan_final commit 시 묶음
- `?? docs/01_brainstorm_v0.6.3/` — 동
- `?? docs/02_planning_v0.6.3/` — 동
- `M docs/bridge_protocol.md` — v0.6.3 정책 박힘. v0.6.3 commit 시 묶음

## 산출물
신규 `scripts/cleanup_v0.6.2_release.sh` (위험 명령 정책 적용).

## 핵심 작업
1. v0.6.2 deploy dispatch md 2건(`stage13_release.md` + `stage13_deploy.md`)을 `dispatch/archive/` 또는 적절한 archive 위치로 git mv
2. `handoffs/active/HANDOFF_v0.6.2.md` → `handoffs/archive/` 변경 git stage (이미 mv 됐으므로 git rm + git add archive)
3. `HANDOFF.md` symlink → `handoffs/active/HANDOFF_v0.6.3.md` 신규 target으로 재연결 (HANDOFF_v0.6.3.md skeleton 신규 생성)
4. 단일 commit: `release(v0.6.2): handoff archive + dispatch archive + HANDOFF v0.6.3 symlink 정리`
5. push는 **운영자 명시 승인 게이트** (위험 명령 정책 — 스크립트 dry-run 후 운영자 `--confirm`)

## 산출물 안전장치 (필수)
- `--dry-run` 기본 / `--confirm` 명시 시 실 실행
- shellcheck CLEAN
- 작업 트리 청결 검증 (v0.6.3 신규 파일은 untracked로 남기되, 본 cleanup 영역 외는 stage 안 함)
- `set -e` + `trap` abort
- 멱등성 (재실행해도 안전)

## 중단 조건
- shellcheck FAIL
- dry-run 검증 실패
- v0.6.3 영역 파일 실수 stage (방어 필요)

## 보고
- dry-run 출력 → 회의창 보고
- 운영자 `--confirm` 후 commit 실행
- push 여부는 별도 운영자 승인 (이미 push 됐으면 skip)

## critical files
- 신규: `scripts/cleanup_v0.6.2_release.sh`
- 신규: `handoffs/active/HANDOFF_v0.6.3.md` (skeleton)
- 정정: `HANDOFF.md` symlink → `handoffs/active/HANDOFF_v0.6.3.md`
- 참조: 기존 `scripts/release_v0.6.2.sh` (위험 명령 패턴)
- 참조: memory `feedback_dangerous_commands.md`

## 자율 영역
- HANDOFF_v0.6.3.md skeleton frontmatter / status / 초기 본문
- dispatch archive 위치 (기존 archive 패턴 따라)
- commit msg 디테일
- 스크립트 인자 / abort 메시지 / 멱등성 구현
- 박지영이 자기 분담 자율 — 직접 / 위임(예: 안영이가 finalizer 끝난 후 처리)

## 우선순위
v0.6.3 plan_final commit과 분리. v0.6.3 commit 전이든 후든 운영자 자율. 본 cleanup이 v0.6.3 진행을 막지 않음.
