---
date: 2026-04-27
version: v0.6.4
stage: 10d (followup)
work: dashboard 디자인 정합 (5개 묶음 fix)
prev: Stage 10c commit efdd532 (이름 충돌 fix APPROVED Score 96/100, release blocker 닫힘)
trigger: 운영자 검증 — dashboard 본문 정상 작동, 단 박스 외곽 깨짐 + PM 누락 + status_bar 미표시 + 텍스트 잘림 + placeholder 누락
---

# Stage 10d brief — dashboard 디자인 정합

> brief만. 본문 / CSS / 위젯 코드 / commit msg / 검증 step = PL 자율.

## 1. 운영자 결정 = 6개 항목 묶어서 fix

1. **박스 외곽 라인 정정** — Pending Push/Commit + Pending Q 둘 다 윗+우측 모서리 끊김 (textual CSS border 또는 위젯 폭 사고)
2. **Pending Q 텍스트 잘림 정정** — Q5 누락 + "외 1건"으로 줄어듦 (박스 안 텍스트 wrap 또는 스크롤)
3. **PM 박스 추가** — 브릿지 = 스티브 리, tmux `bridge-064` 영역 (Stage 10b wiring 시점 누락)
4. **상단 PM 상태 바** (`status_bar.py`) 표시 정합
5. **진행률 바 / 스파크라인 placeholder 표시** — idle 0% 상태에도 빈 바 보이게
6. **★ 실시간 페르소나 트래킹 작동 검증 (Critical)** — 운영자 발견: 공기성(Orc-064-dev:1.1)이 Stage 10d 진행 중인데 dashboard에 idle / tokens 0.0k 표시. = 데이터 collector 자체가 작동 안 함. 원인 후보: (a) PERSONA_TO_PANE 매핑 잘못 (b) `tmux_adapter` / `persona_collector` 본문 stub 상태 (c) token 정규식 / idle 판정 로직 사고. **fix 후 검증 = 실제 활성 페르소나(공기성 등)가 active 상태 + 실측 token 표시**.
7. **★ Pending Q / Pending Push 실시간 갱신 사고 (Critical)** — 운영자 발견: Pending Q 박스에 v0.6.3 `dispatch/2026-04-27_v0.6.3_stage5_design.md` 데이터(Q1~Q6 + "외 1건")가 그대로 남아 있음. v0.6.4 진행 중인데 갱신 0건. = `pending_collector` stub 상태 또는 v0.6.3 dispatch만 읽고 있음. **fix 후 검증 = v0.6.4 dispatch / 본 stage 진행 영역 pending 데이터로 갱신 표시. 옛 v0.6.3 데이터 잔존 0건**.

**= 본질 사고 영역 (6+7번 묶음)**: Stage 10b "wiring 통합" commit (a891d97 Score 93/100)이 사실상 collector 인스턴스만 만들고 실제 fetch 본문은 stub 상태에서 끝난 사고 5 변종 (검증 누락 + 점수만 통과). Stage 9 → Stage 10b → Stage 10c 패턴 3회 반복. **Stage 10d는 본질 fix + PL 직접 실행 검증 PASS 게이트 절대 강제**.

## 2. 운영자 정정 (F-D3 산식, 2회 정정)

**중요 정정 (운영자 명시 + 추가 정정):**
- **PM (스티브 리)** = tmux `bridge-064` 세션 = 표시 + 트래킹 OK
- **CTO (백현진)** = Code 데스크탑 앱 사용 = 회의창 본인 = tmux 외 → **표시 OK, 세션 트래킹 X** (idle 또는 "N/A" 정적 표시)
- **CEO (이형진)** = 실제 사람 = AI 가시화 외 → **표시 OK, 세션 트래킹 X** (idle 또는 "N/A" 정적 표시)
- **HR** = 미정 = **표시 X**

따라서 dashboard 박스 = **18 박스 (PM 1 + 4팀 15 + CTO 1 + CEO 1)**. CTO/CEO 박스는 페르소나 이름 + 정적 상태(idle/N/A)만, tokens / 진행률 / 시그널 추적 0건.

design_final Sec.4 / personas_18 / `bridge_protocol.md` Sec.11.3 산식 정정 = "박스 18 = 4팀 15 + PM 1 + CTO 1 + CEO 1, 단 CTO/CEO 트래킹 미수행, HR placeholder 미표시".

## 3. 컨텍스트

- Stage 10c까지 release blocker 닫힘 (commit `efdd532` Score 96/100 APPROVED)
- 운영자 dashboard 검증 결과 = 본문 작동 PASS, 단 디자인 깨짐
- 운영자 직접 인용: "박스가 완전치 않아"

## 4. 자율 영역 (PL 자율)

- `scripts/dashboard/dashboard.tcss` border 정의 정합
- `scripts/dashboard/render.py` + `team_renderer.py` 위젯 폭 / 레이아웃 정합
- `scripts/dashboard/status_bar.py` 진입점 통합 (상단 표시)
- `scripts/dashboard/pending_widgets.py` 텍스트 표시 정정 (잘림 회피)
- PM 박스 신규 (스티브 리 페르소나 + tmux `bridge-064` capture 영역)
- `scripts/dashboard/personas.py` PERSONA_TO_PANE 매핑 정정 (CTO/CEO/HR 미표시 처리)
- 팀 분담 (백앤드 트리오 + **프론트 트리오 활성화 강제**)
- commit msg / 검증 step

## 5. 검증 게이트 (강제, 본 stage 닫는 기준)

**다음 명령이 운영자 화면에 본문 + 디자인 정합 PASS:**
```bash
cd /Users/geenya/projects/Jonelab_Platform/jOneFlow
venv/bin/python3 scripts/run_dashboard.py
```

본문 = 16명 박스 (PM 1 + 4팀 15) + 진행률 바 + 스파크라인 + Pending 박스 (외곽 정합) + status_bar (상단).

**PL이 직접 실행해서 본문 + 디자인 둘 다 PASS 확인 후에만 commit + flag.** Stage 9 / 10b 패턴(점수만 통과 / 진짜 작동 검증 누락) 반복 회피.

## 6. 중단 조건 (DEFCON)

git push / 외부 API / rm -rf / 비복구 손실 / 보안 위반 / test FAIL > 0 / shellcheck 빨간줄 > 0 / py_compile 실패 / 운영자 결정 필요 부분.

## 7. critical files

- `scripts/dashboard/dashboard.tcss` (CSS border 정의)
- `scripts/dashboard/render.py` + `team_renderer.py` + `status_bar.py` + `pending_widgets.py` (위젯 컴포넌트)
- `scripts/dashboard/personas.py` (PERSONA_TO_PANE 정합)
- `scripts/run_dashboard.py` (진입점 wiring)
- `docs/03_design/v0.6.4_technical_design.md` Sec.4 / Sec.6 (모듈 구조 + 6 메서드)

## 8. A 패턴 강제

drafter ≤ 800 / reviewer ≤ 600 (drafter 본문 직접 수정) / finalizer ≤ 500 (본문 작성 X). drafter v2 X. verbatim 흡수 X.

UI 영역 비중이 커서 **프론트 트리오 활성화 강제** — 백강혁(reviewer) / 김원훈(finalizer) / 지예은(drafter). `bridge_protocol.md` Sec.11.3 헌법 적용. 백앤드 트리오는 idle 또는 보조 (PM 박스 데이터 수집 영역만).

## 9. 헌법 포인터

- A 패턴 = `bridge_protocol.md` Sec.4 표 + Sec.6 사고 14
- 4 panes 강제 = Sec.6 사고 13 + `.skills/ghostty-tmux-ops/SKILL.md` Sec.4
- 부팅 검증 = Sec.8 11항
- 3중 검증 = Sec.8 10항
- 프론트 트리오 활성화 = Sec.11.3 (UI 영역 강제)
- push 정공법 = `.skills/push-signal-watcher/SKILL.md`

## 10. 산출

- finalizer 마감 doc = `docs/04_implementation_v0.6.4/stage10d_final.md` (PL 자율 명명)
- commit = `git_checkpoint.sh "Stage 10d dashboard 디자인 정합" <files>` (push X)
- 완료 flag = `handoffs/active/v0.6.4/stage10d_complete.flag` (시그니처 1줄)
- 결정 / 중단 = `stage10d_decision.flag` / `stage10d_abort.flag`

---

**brief 끝.** PL 자율 + A 패턴 + 프론트 트리오 활성화 강제 + push 정공법 + **운영자 실행 검증 게이트 강제**.
