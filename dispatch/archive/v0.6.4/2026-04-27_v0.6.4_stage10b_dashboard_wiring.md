---
date: 2026-04-27
version: v0.6.4
stage: 10b (followup)
work: dashboard.py M2~M5 wiring 통합
prev: Stage 10 commit 16def78 (M-1/M-2 fix APPROVED Score 94/100)
trigger: 운영자 발견 — dashboard.py M1 scaffold만 남음, M2~M5 wiring 0건 (release blocker)
---

# Stage 10b — dashboard.py M2~M5 wiring 통합 brief

> 본 dispatch = brief만. 본문 / wiring 코드 / commit msg / 검증 step / 산출 위치 = 모두 PL(공기성) 자율.

## 1. 운영자 결정

**(가) 같은 PL에 wiring 추가 작업** (Stage 10 PL + 백앤드 트리오 그대로 + 필요 시 프론트 트리오 동시 활성화).

## 2. 컨텍스트

- 운영자가 `venv/bin/python3 scripts/dashboard.py` 실행 시점 = M1 placeholder Static 1개만 출력 ("jOneFlow 대시보드 — M1 scaffold / 데이터(M2) / 렌더(M3) / Pending+알림(M4) / 18명 매핑(M5) 본문 미구현 / 종료: q").
- `scripts/dashboard.py` 진입점이 M1 scaffold에서 멈춰 있음:
  - `compose()` = M1 placeholder Static 1개만 yield
  - `on_mount()` / `_refresh_loop()` / `_update_widgets()` / `_show_stale()` = "M2~M4 wiring 영역" 주석만 들어 있고 본문 0건
- `scripts/dashboard/` 폴더 모듈 (`status_bar.py` / `team_renderer.py` / `pending_widgets.py` / `pending.py` / `render.py` / `dashboard.tcss`) = 별도 작성됐지만 진입점에서 import 0건 / wiring 0건.
- M2~M5 commit (3c4ade4 / 721916b / 60c38e0 / edef494)이 모듈만 작성하고 진입점 통합 누락.
- Stage 9 review도 못 잡음 (Score 92/100 APPROVED 그대로 통과).

## 3. 자율 영역 (PL 자율)

모든 wiring 본문 / import / collector·notifier 인스턴스 생성 / `call_from_thread` 패턴 / Pending 위젯 통합 / 팀 분담 (백 트리오 + 프론트 트리오, UI 본문은 프론트 트리오 권고 — `bridge_protocol.md` Sec.11.3) = PL 자율.

## 4. A 패턴 강제

drafter ≤ 800 / reviewer ≤ 600 (drafter 본문 직접 수정 권한 + R-N 마커 trail) / finalizer ≤ 500 (본문 작성 X, verdict + Score + AC + 결정만). drafter v2 단계 X. verbatim 흡수 X.

## 5. 중단 조건 (DEFCON)

git push / 외부 API / rm -rf / 비복구 손실 / 보안 위반 / test FAIL > 0 / shellcheck 빨간줄 > 0 / py_compile 실패 / PL 자율 판정 불가 부분.

DEFCON 외 = PL 자율.

## 6. critical files

- `scripts/dashboard.py` (wiring 대상 진입점)
- `scripts/dashboard/*.py` (M2~M5 모듈 본문)
- `tests/test_dashboard_*.py` 5개 (회귀 + 통합 검증)
- `docs/03_design/v0.6.4_technical_design.md` Sec.2 (모듈 구조) / Sec.6 (6 메서드) / Sec.9 (Pending) / Sec.14 (에러 경로)

## 7. 검증 게이트 (release blocker 닫음 기준)

운영자가 `venv/bin/python3 scripts/dashboard.py` 실행 시점에 본문 표시 PASS:
- 19명 박스 (PM 1 + 4팀 15 + 미표시 placeholder 3, F-D3 산식)
- 진행률 바 + 스파크라인 + 페르소나 이름
- Pending Push / Question 박스
- 1초 polling 정상 + 종료 `q` 정상

= M1 placeholder 화면 더 이상 X.

## 8. 헌법 포인터

- A 패턴 = `bridge_protocol.md` Sec.4 표 + Sec.6 사고 14
- 4 panes 강제 = Sec.6 사고 13 + `.skills/ghostty-tmux-ops/SKILL.md` Sec.4
- 부팅 검증 강제 = Sec.8 11항
- 3중 검증 강제 = Sec.8 10항
- push 정공법 = `.skills/push-signal-watcher/SKILL.md`

## 9. 산출

- drafter / reviewer / finalizer trail = `docs/04_implementation_v0.6.4/stage10b_*.md` (PL 자율 명명)
- commit = `git_checkpoint.sh "Stage 10b dashboard.py M2~M5 wiring 통합" <files>` (push X)
- 완료 flag = `handoffs/active/v0.6.4/stage10b_complete.flag` (시그니처 1줄)
- 결정 필요 / 중단 시 = `stage10b_decision.flag` / `stage10b_abort.flag`

---

**brief 끝.** PL 자율 + A 패턴 + push 정공법. 회의창은 회의 모드 + watcher.
