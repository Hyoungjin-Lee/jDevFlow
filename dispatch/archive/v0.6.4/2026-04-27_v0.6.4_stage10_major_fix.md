---
date: 2026-04-27
version: v0.6.4
stage: 10
work: Major M-1 / M-2 fix
session: Orc-064-dev (4 panes, 백앤드 트리오)
prev_stage: Stage 9 commit c32d237 APPROVED Score 92/100
next_stage: Stage 11 자율 압축 → Stage 12 수동 QA → Stage 13 release
---

# Stage 10 Major M-1 / M-2 fix — dispatch brief

> 본 dispatch = brief만. 본문 / 코드 / fix 대상 / commit msg / 검증 step / 산출 위치 / 헬퍼 시그니처 = 모두 PL(공기성) 자율.

## 1. 운영자 결정 (3건 모두 받음)

| 결정 | 값 |
|------|------|
| (1) Stage 12 QA 운영 방식 | **A 수동** (운영자 + 회의창 직접) |
| (2) Major M-1 / M-2 처리 | **B Stage 10 fix** (분기 후 Stage 12 재진입) |
| (3) Stage 11 Strict 검증 압축 | **자율 압축** (PL 판정 trail만) |

## 2. 컨텍스트

- Stage 9 commit `c32d237` finalizer 마감 doc, verdict APPROVED, Score 92/100
- Critical 0 / Major 2 / Minor 4 / Nit 2
- 본 dispatch = Major 2건만 fix. Minor / Nit = v0.6.5로 위임
- Major 본문 위치 = `docs/04_implementation_v0.6.4/code_review.md` Sec.5
  - **M-1**: AC-T-4 spec deviation (옵션 A/B는 review에 명시)
  - **M-2**: PersonaDataCollector 18 process/sec (옵션 A/B/C는 review에 명시)
- closure 영향 0건 (release 막지 않음)

## 3. 자율 영역 (PL 공기성 판정)

다음 모두 PL 자율로 결정·작성·실행:

- M-1 옵션 A/B 비교 + 판정 + fix
- M-2 옵션 A/B/C 실측 + 판정 + fix
- fix 본문 코드 / 헬퍼 시그니처 / 영역 분담 (백앤드 트리오 안)
- commit msg 본문 + 시그니처
- 검증 step (테스트 / shellcheck / py_compile)
- 산출 파일 위치·이름

회의창은 본문 작성 0건. PL 마감 doc 받을 때만 회수.

## 4. 헌법 강제 (포인터)

- **A 패턴** = `docs/bridge_protocol.md` Sec.4 표 "dispatch 작성 정책" + Sec.6 사고 14
  - drafter ≤ 800 / reviewer ≤ 600 (drafter 본문 직접 수정 권한) / finalizer ≤ 500 (본문 작성 X)
  - drafter v2 단계 X, verbatim 흡수 X
- **4 panes 강제** = `bridge_protocol.md` Sec.6 사고 13 + `.skills/ghostty-tmux-ops/SKILL.md` Sec.4
  - `Orc-064-dev` 안 split 4 panes (왼쪽 PL 1 + 오른쪽 stack 3)
  - 페르소나 = 1.1 공기성-개발PL / 1.2 카더가든-be-drafter / 1.3 최우영-be-reviewer / 1.4 현봉식-be-finalizer
  - `@persona` user option + `pane-border-format ' #{@persona} '`
  - Agent tool 분담 X
- **부팅 검증** = `bridge_protocol.md` Sec.8 11항 (4 panes 모두 직접 capture, 1개 보고 4개 추측 X)
- **3중 검증** = Sec.8 10항 (capture + 디스크 + git log)
- **push 정공법** = `.skills/push-signal-watcher/SKILL.md`
  - 완료 시점: `handoffs/active/v0.6.4/stage10_complete.flag` (시그니처 1줄)
  - 결정 필요: `stage10_decision.flag`
  - 중단: `stage10_abort.flag`

## 5. 중단 조건 (DEFCON — 즉시 보고)

- `git push` / `git push --force` / 원격 변경
- `rm -rf` / `git reset --hard` / `git branch -D`
- 외부 API 호출 (비용/영향)
- 보안 위반 / 시크릿 노출
- test FAIL > 0 / shellcheck 빨간줄 > 0 / py_compile 실패
- PL 자율 판정 불가 영역 (운영자 정책 결정 필요)

DEFCON 외 = PL 자율.

## 6. critical files

- `docs/04_implementation_v0.6.4/code_review.md` Sec.5 (Major 본문 + 옵션)
- `docs/03_design/v0.6.4_technical_design.md` Sec.4.3 / Sec.17.1 (M-1 spec)
- 본 dispatch (자가 참조)

## 7. 회수 흐름

```
PL → 통합 verdict + commit + stage10_complete.flag 박음
bridge → flag 회수 + 회의창에 📡 status 전달
회의창 watcher → flag 도착 = task-notification 자발 깨어남 → 운영자 보고
```

---

**brief 끝.** PL 자율 + A 패턴 + push 정공법 적용. 회의창은 회의 모드 + watcher.
