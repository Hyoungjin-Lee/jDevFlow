# Stage 9 brief — 최우영 (be-reviewer, Opus high, Orc-063-dev:1.3)

**상위 dispatch:** `dispatch/2026-04-27_v0.6.3_stage9_review.md`
**상위 input:** Stage 8 commit chain `5ac2e16` + `868de75` + `ea2fd16`
**참조:** `docs/03_design/v0.6.3_stage9_review_sketch.md` (본인 작성, 검토 포인트 20건)
**참조:** `docs/03_design/v0.6.3_design_final.md` (Score 89.9%, AC 26/21자동/5수동)
**산출물:** `docs/04_implementation_v0.6.3/code_review.md`

---

## 분기 판정 (공기성 1.1 자율)

- 환경: Codex CLI 발견 (`/opt/homebrew/bin/codex`) → F-62-8 모드 A trigger 매칭
- 운영자 결정 #15: **v0.6.3 = conditional 인터페이스(분기 로직)만 / plugin-cc 자동 호출 고도화 = v0.6.4 후속**
- 외부 API 자동 호출은 자율 권한 외 → **본 stage = self-review 진행** + Codex 분기 인터페이스 동작 검증
- 본 결정 trace는 code_review.md Sec.0 또는 Sec.1에 명시

## 컨텍스트

- 자율 모드 — 운영자 보고 X (Stage 13만)
- Stage 8 AC 21/21 회귀 PASS 이미 확인 (회귀 0건)
- 속도 우선, thinking medium, sketch 20건 그대로 활용

## 핵심 작업

### 1. Stage 8 산출물 cross-check (sketch MR1-1 ~ MR4-8 = 20건)

각 검토 포인트 별로 실 commit 대상 cross-check:
- **M1 (12건):** monitor_bridge.sh / setup_tmux_layout.sh / update_handoff.sh / hook_post_tool_use.sh
- **M4 (8건):** spawn_team.sh / .claude/settings.json / ai_step.sh

검토 결과를 `통과 / 보강 / 회귀` 3분류:
- 통과: 검토 포인트 정합
- 보강: design 의도와 미세 차이 (Stage 12 QA 또는 v0.6.4 이월)
- 회귀: AC 또는 design 명시 위반 (Stage 10 패치 자동 진입)

### 2. 모드 A 분기 인터페이스 동작 검증

```bash
# 본인이 직접 실행해서 검증
bash scripts/ai_step.sh --status 2>&1 | grep -i "stage9\|codex\|review" || true
# 또는 stage9_review 분기 함수 직접 호출 (있을 시)
```

설계대로 "Stage 9: Codex review 경로 활성화" 메시지 출력 확인 → trigger PASS.

### 3. AC 회귀 재검증

```bash
bash tests/v0.6.3/run_ac_v063.sh
# 결과 21/21 = 회귀 0건 / <21 = 회귀 발생
```

회귀 발생 시 → 본인(공기성 1.1) 즉시 capture 보고 + Stage 10 패치 자동 분기 권고.

### 4. shellcheck / py_compile 회귀 검증

```bash
shellcheck scripts/monitor_bridge.sh scripts/update_handoff.sh scripts/setup_tmux_layout.sh scripts/ai_step.sh scripts/spawn_team.sh scripts/hook_post_tool_use.sh tests/v0.6.3/run_ac_v063.sh
# info-level은 OK (gradual adoption Q3), error 발생 시 보강 또는 회귀
```

## 본문 골격 (`docs/04_implementation_v0.6.3/code_review.md`)

```
---
version: v0.6.3
stage: 9 (code_review)
date: 2026-04-27
authored_by: 최우영 (개발팀 백엔드 리뷰어, Opus/high, Orc-063-dev:1.3)
upstream:
  - Stage 8 commit chain: 5ac2e16 + 868de75 + ea2fd16
  - docs/03_design/v0.6.3_stage9_review_sketch.md (본인, 20건)
  - docs/03_design/v0.6.3_design_final.md (Score 89.9% gate PASS)
mode: self-review (모드 B fallback) — 분기 trigger 인터페이스는 모드 A 매칭, plugin-cc 자동 호출은 v0.6.4 후속 (운영자 결정 #15)
session: 27
---

# Sec. 0. 리뷰 개요 + 분기 판정 trace
# Sec. 1. M1 cross-check (MR1-1 ~ MR1-12)
# Sec. 2. M4 cross-check (MR4-1 ~ MR4-8)
# Sec. 3. 모드 A 분기 인터페이스 동작 검증
# Sec. 4. AC 회귀 + shellcheck 회귀 검증 결과
# Sec. 5. 보강 영역 (B-시리즈 / Stage 10 패치 후보 또는 v0.6.4 이월)
# Sec. 6. Stage Transition Score (Stage 9 자체 평가, plan_final 가중치)
# Sec. 7. APPROVED / REJECTED 종합 판정 + Stage 12 진입 권고
```

## Score 항목 (Stage 9 자체)

| 항목 | 가중치 | 평가 기준 |
|------|-------|---------|
| (1) Design 정합성 | 30% | sketch 20건 cross-check 통과율 |
| (2) AC 회귀 | 20% | 21/21 PASS = 100점, 회귀 1건당 -10점 |
| (3) shellcheck/py_compile | 15% | error 0 = 100점 |
| (4) F-62-x 흡수 | 20% | 9건(F-62-1~9) 본문 흡수 검증 |
| (5) 보강 영역 명료성 | 15% | Stage 10 vs v0.6.4 이월 분류 명확성 |
| **합계** | **100%** | |

**Stage 12 게이트:** Score ≥ 80% + AC 회귀 0건 + 운영자 결정 영역 0건 → APPROVED.

## 작업 지시 + 마커

1. sketch 20건 cross-check (실 commit 대상 직접 검증)
2. AC + shellcheck 회귀 직접 실행
3. code_review.md 작성 (보강 영역 명료 분류)
4. 마지막 라인: `COMPLETE-REVIEWER-S9: <줄수> lines, verdict=APPROVED|REJECTED, score=<X>%, AC=<통과>/<전체>, 보강=<N>건, file=docs/04_implementation_v0.6.3/code_review.md`

## 자율 영역

- code_review.md 형식 (자유, v0.6.2 패턴 참고 가능)
- 보강 영역 우선순위 (Stage 10 vs v0.6.4)
- Score 추정 점수 (근거 명시)

## 중단 조건

- AC 회귀 발생 → 본인(공기성 1.1) 즉시 보고 (Stage 10 자동 진입 권고)
- shellcheck error 다수 → 본인 보고
- Codex 분기 인터페이스 동작 모순 → fallback 강제 + 본인 보고

## 보고

완료 시 `COMPLETE-REVIEWER-S9` 마커. 본인이 verdict 흡수 → 박지영(Orc-063-plan:1.1) 보고.

운영자 보고 X (자율 모드).
